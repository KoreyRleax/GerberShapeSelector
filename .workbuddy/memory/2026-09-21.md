# 2026-09-21

## GerberParserSmartV4.0 —— 多图层选点两处 bug 修复

用户报了两个现象，根因都出在**多图层叠加后的判定口径**上。

### 现象 1：选点只能选到"最大圆圈"那个图层
- **根因**：`ShapeQuery.HitTest` 只按"点到边界距离最近"取胜者，而**圆内部的边界距离 = `len - r`，
  直径越大这个值越小** → 同一位置上的大圆永远赢过压在它上面的小图形。
  真实板子上 GBL / GBS 常在同一坐标放直径差很大的焊盘，于是"不论点哪儿，选中的都是大圆那层"。
- **修**：`HitTest` 增加可选参数 `Func<SelectedCircle,int> layerDepth`（**数值大者靠上层**），
  排序键改成**两级**：先比叠放，同一层内再比距离。
  新增 `ShapeQuery.SkipDepth`（`int.MinValue`）= 该图形本次不参与命中。
- **方针由用户拍板**：**穿透** —— 最上层在该处没有图形就落到下一层；
  不采用"仅最上层可点"（那会让下层整层点不动）。
- **层序口径由用户拍板**：按**画面最上层**（= 后加载 = `_layers` 末位）。
  图层容器列表顺序**保持不动**（它第一行是最先加载的、画在最底层的那层，与画面层序相反 —— 已记进文档遗留）。

### 现象 2：图层隐藏后，它的选点还画着、还能点中
- **根因两处**：① 选点是**独立于底图**画的第二遍 —— 底图那一路（`DrawGerberApertures`）过滤了显隐，
  `DrawSelectedCircles` / `DrawAllTemplateCircles` 这两处漏了；② 命中测试只认几何、不认图层。
- **修**：新增 `IsShapeLayerVisible()` 统一口径，接在四个地方：
  绘制 2 处 + 命中（`DepthForHitTest` 返回 `SkipDepth`）+ 框选（`GetCirclesInSelectedRegion`）
  + 多选扩散（`HandleMultipleSelect` 搜索范围的第一道过滤）。
- **数据不删**：`_selectedCircles` 与 `_apertures` 都不动 → 包围盒不跳、视图不晃；
  重新勾上图层，选点原样回来（隐藏是临时视觉操作，不该顺手删掉用户选的几千个点）。
- **顺带修**：多选扩散的第二道过滤原来写的是 `allShapes.Where(...)`（在**原集合**上过滤，
  会覆盖掉第一道），改成链式 `searchSource.Where(...)` —— 两道过滤才是叠加关系。

### 新增的图层查找表
`_layerDepthMap`（图层名 → 序号）+ `_hiddenLayerNames`（隐藏集合），由 `RebuildLayerLookup()` 重建。
调用点两处：`RefreshLayerPanel()`（覆盖所有载入 / 切换工程路径 —— 调用位置刻意放在**早退之前**）、
`OnLayerRowVisibilityChanged()`。
作用：把"这个图层可见吗 / 排第几层"从 O(图层数) 的字符串比较降到 O(1)（1.5 万图形 × 10 图层 = 15 万次/帧）。
**漏调的症状是"滞后"而非崩溃** —— 图层刚隐藏，点击却还能选中它。

### 踩到的坑：Edit 工具会把整个文件写成 LF
`ShapeQuery.cs` 改完变成**全 LF**（216 行），而本仓库 `core.autocrlf=true` → 工作区应为 CRLF
（`git ls-files --eol` 显示 `w/lf`）。用 PowerShell 归一（先 `\r\n`→`\n`，再 `\n`→`\r\n`）
+ `UTF8Encoding($true)` 写回，复验为 `w/crlf`、BOM 保留。
**但 `MainForm.cs` 同样走 Edit 却保持住了 CRLF** —— 说明这不是必然，**每次改完都要用
`git ls-files --eol` 复验**，不能想当然。
（`GerberParserV4.0_交接文档.html` 在仓库外，本身是纯 LF + 无 BOM，不受影响。）

### 验证
- `dotnet build GerberParserSmartV4.0.csproj -c Debug -p:Platform=x64 -p:OutputPath=<tmp>`
  → **0 warning / 0 error**（1.6 ~ 2.6 s），跑了两次（代码改完、行尾修复后）。
- 输出刻意落到 `C:\Users\59538\.workbuddy\tmp\GPV4_build\`，
  **完全没碰 `bin\x64\Debug\Json\`**（真实模板数据）。
- `git status` 干净：只有 `MainForm.cs`（+127 行）、`ShapeQuery.cs`（+44 行）两个改动，
  无多余临时文件。

### 文档
`GerberParserV4.0_交接文档.html` 更新：顶部加 09-21 修复块；第五节新增第 10 / 11 条设计决策；
第六节新增「多图层：谁被点中 / 什么看不见」速查表；第八节修正"图层面板是空的"这条已过时遗留
（图层容器已实现在画布右上角），并新增"容器顺序与画面层序相反"这条遗留；
代码地图行数刷新为实测值（MainForm ~3720 / ShapeQuery ~215 / Designer ~450）。
标签配对自检全 OK（div/table/tr/td/th/ol/ul/li/p/h2/h3/h4/pre/code/b/span 全部 open == close）。

---

## 第二轮（同一天 —— 用户反馈"仍然只能选最大圆圈的"之后）

### 用户新增的两个诉求
1. 图层容器里加**「置顶」按钮**：点击后该图层置顶，并在列表排到首位；
2. **同类扩散只在当前点击那一层内**进行。

### 第一版修复为什么可能没生效：拿"图层名字符串"关联图形与图层太脆
第一版是"用 `shape.Layer` 去 `layer.FileName` 匹配出来的字典里查深度"。
只要有一处对不上（老数据、命名差异、以后改命名规则），**所有图形都成了"未知图层"**，
`DepthForHitTest` 一律返回 -1（全相同）→ **静默退回纯距离排序** → 又回到"怎么点都只选到大圆"，
而且没有任何报错可查。

已核实的两点（说明不是名字源头的问题，而是这条链路本身不该赌）：
`displayName = Path.GetFileName(path)`（与 `LayerInfo.FileName` 同源）；
`GerberParser.ParseFile` 开头就 `Reset()` 清了 `apertureDict`，所以不会跨层污染 `Layer`。

**改法：状态直接落在图形对象上，全链路不做字符串匹配。**
- `SelectedCircle` 新增三个运行期字段（属性初始化器，不进 circles.json）：
  `LayerId` / `LayerDepth` / `LayerHidden`
- `LayerInfo` 新增 `Id`（会话内唯一，**置顶重排不变**）与 `ColorIndex`（配色，同样不随重排变）
- `BuildAllShapes` 用 `Dictionary<Aperture, LayerInfo>`（**对象引用**做键）建立归属，写 `LayerId`
- `RefreshLayerState()` 统一把深度 / 隐藏状态拷进每个图形；
  调用点：`RefreshLayerPanel()` 的早退**之前**、`OnLayerRowVisibilityChanged()`、`MoveLayerToTop()`
- `DepthForHitTest(s)` = `s.LayerHidden ? SkipDepth : s.LayerDepth`（纯字段读取，无匹配）
- 构建日志补了"**归属失败 N 个光圈**"—— 非 0 就说明归属断了，这是最快的自检信号

### 新增：图层置顶
- `LayerRowControl` 加「置顶」按钮：整行是自绘的，再切一块区域出来。
  **从 `OnClick` 换成 `OnMouseDown`** —— 一行里有两个可点区域，必须按坐标分流，
  Click 在 MouseUp 才触发、拿不到可靠的分区语义。
  已是最上层的那行**不画按钮**（点了没作用，画出来只会误导），但**位置照留**，保证复选框纵向对齐。
- `MoveLayerToTop(idx)`：把图层移到 `_layers` **末位**（= 最后绘制 = 压在最上面）。
- **容器改为倒序填充**：首行 = `_layers` 末位 = 画面最上层，与画面叠放一致 ——
  这样"置顶 = 排到首行"这句话才成立（原来首行是最底层，两者恰好相反）。
- 行宽统一取最大值（`MeasureRowWidth` 改 `public static` 供 RefreshLayerPanel 调用），否则复选框左右参差。

### 顺序持久化（不然每次开工程都要重新置顶）
- 保存端 `TemplateInfo.Layers` 加 `ColorIndex`（**v2.3**）；数组顺序本身就是图层顺序。
- `ApplyLayerVisibility` 重写：按清单顺序**重排 `_layers`**，再恢复显隐与 `ColorIndex`；
  清单里没提到的图层（工程目录里新加的文件）保持相对顺序追加到后面。
  老文件没有 `ColorIndex`，dynamic 访问会抛 `RuntimeBinderException` → 单独 try 包住，退回 -1。

### 同类扩散限层
`HandleMultipleSelect`：`c.ID == clickedShape.ID && c.LayerId == clickedShape.LayerId`。
光圈 ID 只在单个 Gerber 文件内有意义 —— GBL 的 D10 与 GTS 的 D10 是两个不同的焊盘。

### 诊断日志（排查完可整段删除）
- `LogHitDiagnostics(x, y, tolerance, chosen)`：列出光标附近图形的"图层#深度"，**按深度降序**排，
  命中者应当是第一个 —— 不是第一个就是优先级有问题；所有深度都一样就是归属没接上。
  还会报"图层总数"（= 1 时特别标注，那种情况下"叠放优先"无从谈起）。
- `LogLayerOrder()`：载入工程、置顶后各记一次"图层叠放顺序（从最上层到最下层）"。

### 验证
`dotnet build -c Debug -p:Platform=x64 -p:OutputPath=<仓库外 tmp>` 共跑 4 次（每轮改动后），
全部 **0 warning / 0 error**；`git ls-files --eol` 复验 `MainForm.cs` / `ShapeQuery.cs` 仍为 `w/crlf` + BOM。

---

## 第三轮（用户跑了回归测试 + 提出图标化 + 追问 JSON 结构）

### 回归测试结果（`bin\x64\Debug\logs\2026-09-21.log`）
**分层命中确认生效 ✓**。日志里"选点诊断"整片都是 `→ 命中 top.o#5`（最上层），
置顶后立刻变成 `→ 命中 smt.o#5` / `drl.o#5`，与置顶操作严格对应 ✓。

实测时间线（同一块板，6 图层 / 20188 图形）：
- **载入工程**：05.992 → 06.566 = **574 ms**，其中解析 6 个文件 410 ms、
  `RebuildAperturesFromLayers + BuildAllShapes + RefreshLayerState` 合计 **12 ms**
- **恢复选点**：09.525 → 09.727 = 202 ms，其中 **150 ms 白花在一个异常上**（见下）

### 日志暴露的真缺陷：图形唯一键缺"尺寸"这一维
日志指纹：`从 circles.json 恢复 2 个选点` + `复用选点对象 **8** 个` —— **复用数 > 已选数**，
说明同一个选点对象被反复认领、真正的图形整个消失。

根因：`ShapeKey = 图层 + 坐标 + 形状`，**没有尺寸**。同一图层、同一坐标、同一形状、直径不同的
两个圆（焊盘与它的外形框常常都落在中心点上）会算出同一个键 → 建索引时互相覆盖。
后果不是"少画一个"而是**错位认领**，症状是"那个点怎么都点不中"。

修：
- 新增 `GetSize(shape, diameter, width, height, rotation, out sizeX, out sizeY, out sizeR)`
  统一把各形状的尺寸折成三元组（圆→直径,直径,0；矩形→宽,高,0；椭圆→宽,高,旋转）
- `ShapeKey` → 7 元组（+ sizeX/sizeY/sizeR），坐标也统一 `Math.Round(...,4)`（与保存端同精度，
  于是 json 恢复的选点也能走**精确键**，不再只靠 loose 键）
- `ShapeKeyLoose` 同步加尺寸
- 各加两个便捷重载：`ShapeKey(SelectedCircle)` / `ShapeKey(Aperture a, double x, double y)`
- **`usedSelected` 兜底**：`HashSet<SelectedCircle>`，保证每个选点对象最多被复用一次，
  键即使再撞也只会"多建一个图形"而不是"重复认领"
- `IsSingleShapeInShapes` 补 **LayerId** 维度（原来只比坐标+形状+尺寸，跨图层同坐标的点会被
  判成"已经选过"而加不进 `_selectedCircles` → 选了保存不进去）
- 构建日志加自检：`复用数 > 已选点数` 时追加 ⚠ 提示

### "加载慢 150 ms"是我自己引入的（已修）
`ApplyLayerVisibility` 里用 `try { layer.ColorIndex = (int)item.ColorIndex; } catch { ... }` 兜
"v2.3 才有的字段"。打开**老工程**时每个图层抛一次 `RuntimeBinderException` ——
异常构造 + DLR 绑定失败都很贵，6 个图层 = **150 ms**，正好压在"打开工程"的关键路径上。

修：改用 `JToken` 的**索引器**取字段（`item["ColorIndex"]` 取不到返回 null，不抛异常），
顺带把 `IsVisible` 也改成类型检查后取值。需要 `using Newtonsoft.Json.Linq;`。
方法签名仍是 `dynamic`（调用方传的就是 JObject，`as JToken` 成功）。

> 教训：**用异常探测"字段在不在"是昂贵的**。Newtonsoft 的 JToken 索引器天生返回 null，
> 没有理由用 dynamic 点属性 + try/catch。

### 置顶按钮 → 图标
`LayerRowControl` 里的「置顶」文字换成**几何自绘的向上箭头**（实心三角头 + 短箭杆，
`FillPolygon` + `DrawLine`）—— 不赌系统装了哪个字形，也不受 DPI 缩放影响。
按钮宽 34 → 24 px。加了 `ToolTip`（字段 `_layerRowToolTip`，在 `BuildLayerPanel` 建）补文字说明，
因为图标本身没有语义。行宽仍统一取最大值，最上层那行只是不画图标、位置照留。

### JSON 导出结构（用户问）
**是多图层的**：`Shapes.Circles/Rectangles/Ovals` 每条记录都带 `Layer`（图层文件名），
一条记录一个图形一个坐标；`TemplateInfo.Layers` 数组顺序 = 图层绘制顺序。
另：`Version` 从 "2.2" 升到 **"2.3"**（因为 Layers 元素补了 `ColorIndex`）；
读取端不校验版本号，老文件照样能开。

### 验证
本轮又编译 2 次（改动后 + 异常修复后），均 **0 warning / 0 error**；
行尾复验 `w/crlf` + BOM；文档标签配对自检全 OK。

---

## 第四轮（用户指示：学习 RBLAOI 的文档规则并模仿）

### 学到的规则（来源：桌面 RBLAOI，**只读它的 MD，未动它任何文件**）
`C:\Users\59538\Desktop\RBLAOI\RBLAOI\MD文件汇总（AI）\` 这套体系由用户 2026-09-17 亲自定的
（见其中的 `任务&需求/R-20260917-05-文档体系重构.md`，含用户原话），要点：

- **四类文档 + 索引入口**：`待办/`（完成作废保留标注）/ `任务&需求/`（**必须有验收目标**）/
  `问题&解决方案/`（**按问题点命名**，一个问题一个文件）/ `系统架构&协议设计/`（基本确定后不动）；
  入口是该目录的 `README.md`
- **与"给 AI 的材料"分开**：RBLAOI 有 `开发日志/`（交接文档 + 更新日志）给 AI；
  面向人的结论进 `MD文件汇总（AI）/`
- **路径写法**：文档互相引用**一律从仓库根写全**
- **三件事优先级**（用户原话）：①我做了什么（需求）②怎么做（解决方案）③为什么这么做（技术实现）
- **三层保障**让别的会话也守规则：`CLAUDE.md`（跨工具）+ 各目录 README（就近）+ **只读巡检脚本**（兜底）
- 巡检脚本 `检查文档归置.ps1` 检查：目录结构 / 漏置 md / 需求缺"验收目标" / 问题缺"根因+解决方案" /
  README 未登记 / 文档引用未从仓库根写全

### 本工程的落地（13 个新文件 + 1 个脚本）
在 `GerberParserV4.0/MD文件汇总（AI）/` 下建同构体系：

| 位置 | 文件 |
|---|---|
| 根 | `README.md`（索引 + 规则 + 与 RBLAOI 的差异：本工程**不建 `开发日志/`**，因为它已被 `.workbuddy/memory/` + 交接文档 HTML 承担）、`检查文档归置.ps1` |
| `待办/` | `README.md`（规范 + 状态五档）、`待办清单.md`（T01~T03 待你操作 / T04~T10 技术债 / T11~T12 暂缓 / T20~T28 已完成） |
| `任务&需求/` | `README.md`、`R-20260921-01-多图层选点与图层顺序.md`（**本轮主需求，含 14 条可判定验收目标**）、`历史需求索引.md` |
| `问题&解决方案/` | `README.md`（规范 + 历史问题索引）、`选点只能选到最大圆圈那一层.md`、`隐藏图层的选点仍然可见可点.md`、`重复坐标的图形互相顶替.md`、`打开工程慢150毫秒（用异常探测字段）.md`、`沙盒环境与工具链.md` |
| `系统架构&协议设计/` | `README.md`、`系统架构总览.md`（代码地图 + 数据流 + 图层模型 + **11 条关键不变量**）、`数据格式-circles.json.md`（含 v2.0→v2.3 演进与去重口径） |

**适配判断**（不是照抄）：
- RBLAOI 的 `开发日志/` → 本工程不建，README 里写明由 `.workbuddy/memory/`（过程）+
  `../GerberParserV4.0_交接文档.html`（会话级交接）承担
- RBLAOI 有"协议"类文档 → 本工程是**离线工具**，没有协议；**与下游的契约就是 `circles.json`**，
  单独写成 `数据格式-circles.json.md`
- 巡检脚本的"允许位置"改为：`MD文件汇总（AI）\*` 与仓库根 `README.md`（`.workbuddy\` 已排除）

### 自检
- 巡检脚本跑完 **通过**（首轮报出 `沙盒环境与工具链.md` 缺"根因/解决方案"两个必备段 → 已按规范补结构）
- 全部新文件 **CRLF**；`.ps1` 加 **BOM**（PowerShell 5.1 读无 BOM 的 UTF-8 会按 GBK 解析，中文匹配会失效）
- 规则已写进 `.workbuddy/memory/MEMORY.md` 的「四、文档体系与索引」（三层保障的第一层）

### 待用户决定
- 要不要也建一个仓根 `CLAUDE.md`（RBLAOI 的第一层机制，跨工具让任何 AI 会话都读到规则）？
  目前规则只写在 `.workbuddy/memory/MEMORY.md` 里，够我自己用，但对"别的 AI 工具"不可见。

---

## 第五轮（用户裁定：规则层放根 README，不建 CLAUDE.md）

> 用户原话："可以，但应该把 claude 改为 readme，根目录的"

### 做了什么
在 `GerberParserV4.0/README.md`（仓库根，原有文件）的**引言之后、正文之前**插入一节
「🔴 动这个工程之前先读这一节（跨工具约定）」，含两块：

1. **〇、文档归置规则**：四类文档各放哪 + 硬性要求（需求必须有验收目标、问题必须有根因+解决方案、
   待办完成保留标注、架构标来源行号）+ 三条配套约定（登记进索引 / 引用从仓库根写全 / 跑巡检脚本）
2. **〇之二、给 AI 会话的环境警告**：沙盒**可编译**（与 RBLAOI 相反）/ Edit 会改行尾 /
   Bash PATH 坏 / PowerShell stdout 不稳 / Edit 不能并行 / 临时文件写仓库外 /
   `bin\x64\Debug\Json\` 不可再生 / 不要写 `.git/refs/**`

### 为什么放根 README 而不是 CLAUDE.md
同样实现"跨工具可见"：任何 AI 或人打开工程，第一眼就是根 README。
且本工程原本没有 `CLAUDE.md`，凭空多一个文件名反而多一层约定要去解释。

### 自检
- `git diff --numstat README.md` = **51 插入 / 0 删除** —— 确认是**纯插入、没动原有内容**
- ⚠ 发现 `README.md` **原本工作区是 LF**（293 行 LF、0 CRLF，与仓库 `core.autocrlf=true`
  → 工作区应为 CRLF 的约定不符）。已按约定归一为 CRLF；`git diff` 仍只显示 51 行插入，
  说明 autocrlf 下 git 先规范化行尾，**行尾转换没有造成全文件 diff** ✓
- 巡检重跑 **通过**；`MD文件汇总（AI）/README.md` 的"三层保障"第一层已改为**根 README**，
  目录树与"工程根"索引同步更新
- 规则同步写进 `.workbuddy/memory/MEMORY.md` 的「四、文档体系与索引」

### 顺手发现（未改，待用户决定）
根 `README.md` 正文里有几处**已过时**（它是 2026-09-19 那轮的设计裁定稿）：
① §1.2 说"图形唯一键升级为 `(LayerIndex, X, Y, Shape)`" —— 实际用的是**图层文件名**，且
2026-09-21 又补了**尺寸**维度（现在是四维）；
② §1.4「R2 同位置多图形默认联动选中」—— 实际实现是**按图层叠放优先**（最上层赢、可穿透），
不是"整组联动"；§五-1"同坐标去重保留**尺寸最小**者"与实现（保留被点击的那个）也不一致；
③ §四"`GerberParserTool` 引用指向 `bin\Debug\`…建议挪进 `lib\`" —— 2026-09-19 已挪；
④ 文件末尾残留一行孤立的 `# GerberShapeSelector`。
**均未改动**（用户只要求加规则节），已在回复里列出。

---

## 第六轮（用户："更新README"）

把根 `README.md` 的正文更新到**与现状一致**（它是 2026-09-19 的设计裁定稿，多图层改造后已多处过时）。

**改动前先核对了代码事实**（不臆造）：
- 菜单结构 → `MainForm.Designer.cs` 的 `.Text = "…"`（菜单组是 **编辑(&E)**，不是"操作"；
  "参数设置"在**视图**下；"操作说明"在**帮助**下）
- 悬浮工具栏按钮 → `BuildOverlayToolbar()`：`单选 多选 | 框选区域 (Q) 解除限定 | 清空选点 底图 | F1 操作说明`
- 删除工程 → `OnDeleteProjectClick()`（1537 行起）：**连目录一起删 + 走回收站**，五条约束
- 对象模型 → 实际是 `MainForm._layers`（`LayerInfo`）+ `_selectedCircles`，没有 `Project`/`Layer` 类

**改了 13 处**：
1. 文件头：加**最后核对日期 2026-09-21** + 指向 `MD文件汇总（AI）/…/系统架构总览.md`、`数据格式-circles.json.md`；
   声明"以代码为准，本文与代码不符是本文过时"
2. §1.2 对象图：`Project/Layer` → 实际的 `MainForm` 字段 + `LayerInfo`（含 `Id`/`ColorIndex`）；
   新增 `SelectedCircle` 四个图层字段表（区分"持久化 / 运行期"）
3. §1.2 唯一键：`(LayerIndex,X,Y,Shape)` → **四维**（图层**文件名** + 坐标 + 形状 + **尺寸**）
4. §1.3 R1：改为"隐藏图层 = 不画 + 不选 + **数据保留**（仍会导出）"
5. §1.3 R2：改为"按图层叠放决定谁被点中（最上层优先、可穿透）"
6. §1.4 整节重写：原始"整组联动 + 按坐标去重"被标记为**已被实现取代**，改述现状与两条"为什么不做"
7. §1.5：`ParseFile` 硬解析问题标 **✅ 已修（2026-09-19 严格模式重载）**
8. §1.6：删掉 v2.0 旧示例，改为**指向 `数据格式-circles.json.md`** + 只留两条改动须知
9. §1.7 三操作表：删除工程改为"连目录删（走回收站）"；新建/打开按实现补齐
10. §二 界面约定：ASCII 图与菜单结构按实际重写（编辑(&E) / 视图 / 帮助；工具栏按钮清单；
    **图层容器不是 AttachOverlayToolbar 挂的、没有视口让位**；状态栏三个信息位）
11. §三：DPI 那条标 **⚠ 尚未实施**（`App.config` 里没有声明）
12. §四：依赖表 `GerberParserTool` 指向 **`lib\`**（已挪）；雷区两条标 ✅ 已修并附新注意点
    （`LangVersion` 只在 x64 → 高版本语法在 AnyCPU 下编不过）；关键设计决策**补 4 条 09-21 的**
13. §五：第 1 条"保留尺寸最小者"改为**实际口径**（四维唯一键去重，不按坐标）；第 2 条补第⑤条约束；
    第 3 条补"仍会被导出"；**删掉文件末尾残留的孤立标题 `# GerberShapeSelector`**

**新发现并记入待办的一条**（T13）：原 README 断言"同一坐标出现两次 = 插两次针，属交付缺陷"，
但实现**没有**按坐标去重（不同图层同坐标是两个点）—— 两处矛盾。
**需确认下游插针机上位机读到同坐标多条时的行为**：重复插针 / 自己按坐标去重？
若会重复，就要在导出端加去重策略（且先定保留哪一条）。
→ 已写入 `MD文件汇总（AI）/待办/待办清单.md` T13，并在 README §1.4 / §五-1 标 ⚠️。

**顺带修正**：上一轮把已完成项 `T03` 放在了"待你操作/决策"节 → 移到"已完成"节。

**自检**：`git diff --numstat README.md` = **191 插入 / 104 删除**（局部改动，非整文件重写）；
行尾 `w/crlf`、无 BOM；巡检**通过**；文件 344 → 329 行（删掉了 v2.0 JSON 示例与残留标题）。

---

## 第七轮（用户：图层容器加顶部栏 —— 文件名 / 置顶 / 【全选】）

### 需求原话
> 图层容器中新增顶部栏，文件名 置顶 【】；前两个只是文字，【】是全选/取消全选，**此项更新图层**。

### 做法（改动**只在 `MainForm.cs` 一个文件**）
1. **新增 `LayerRowLayout`（静态类）** —— 把容器里"三列自右向左"的几何抽成**一份**：
   `BoxSize` / `TopColumnWidth` / `TextWidth()` / `RowWidth()` / `TopColumn()` / `CheckBox()` / `TextAreaWidth()`。
   表头与图层行都调它。**这是本需求唯一有技术含量的地方**：两个控件各算各的列位置，
   差 8px 就错开一格，而这类错位**只看代码看不出来**、只能靠肉眼发现 —— 抽成一处后结构上不可能错开。
2. **`TopColumnWidth` 是量出来的**：`max(24, 「置顶」文字宽 + 6)`。
   原来那列只为 ↑ 图标留了 24px，而 `SystemFonts.DefaultFont` 下两个汉字约 22~26px ——
   写死会裁字或压到复选框上，并连带把每行的文字区宽度也算错。
3. **新增 `LayerHeaderControl`**：`Control` 自绘（与图层行同一套 `SupportsTransparentBackColor`）。
   - 三段：左「文件名」、中「置顶」、右方框；文字比图层名淡一档（×0.72 alpha，黑底上对比度仍 ≥ 9:1）
   - **热区只在方框周围**（12px 框外扩 6px = 24×24）；光标只进热区才变手型；
     点「文件名」/「置顶」无任何反应 —— 整行可点会让"想看清表头"的一次误触**把所有图层全隐藏**
   - **方框三态**：全勾 → 打勾；部分勾 → 一横；全不勾 → 空。
     两态在"6 层勾了 4 层"时会显示成空或勾，**属谎报**
   - 点击行为只有两条：全勾时点 = 全部取消；其余（含半勾）点 = 全部勾上
4. **`RefreshLayerPanel()`**：表头**最先 `Controls.Add`**（TopDown 里先加的在上 = 首行）；
   `maxRowWidth` 初值改为 `MeasureRowWidth("文件名")`，保证表头不比任何一行窄。
5. **`SetAllLayersVisible(bool)`**：遍历设 `IsVisible` → `KLog` + `LogLayerOrder()` →
   `RefreshLayerPanel()`（**重建**）→ `RefreshKWindow()` → 状态栏。
   与单行切换同一套 R1 语义：**不碰 `_selectedCircles` / `_apertures`**。

### 为什么全选要重建容器、单行不用
每行的复选框是**该行控件的私有字段**（`LayerRowControl._checked`）。全选改了所有图层，
就必须让所有方框跟着变 —— 唯一同步手段是重建。单行被点的那一行自身已是最新，
重建反而会清掉鼠标下的悬停态（手感变差），所以它只 `RefreshLayerState` + `RefreshKWindow`。

### 被否掉的方案
- `FlowLayoutPanel` + `Label`/`CheckBox` 拼表头 → `CheckBox` 不支持透明背景（黑画布上一块方块），
  且内部留白固定，无法与自绘行逐像素对齐
- 把"全选"做成画布顶部悬浮工具栏上的按钮 → 离容器远、语义不搭（那条工具栏是高频看图动作的地盘）
- 整行可点 = 全选 → 误触代价是"全部图层消失"级别的视觉冲击
- 方框只做两态 / 点击改成"反选" → 分别见上（谎报）与用户原话（要的是全选）

### 踩到的两个小坑
1. **`Write` 工具写 md 是 LF**：本次新建的 `R-20260921-02…md` 是 LF，而同目录其它 md 全是 CRLF
   → 用 node 脚本归一（`\r\n`→`\n`→`\r\n`，UTF-8 无 BOM）。
   **⚠ 这里特意用 node 而不是 PowerShell**：PS 5.1 解析**无 BOM** 的 .ps1 会按 GBK 读，
   脚本里只要出现中文**路径字面量**就会错 —— 文本替换类活交给 node 更稳（node 源码默认 UTF-8）。
2. **根 README 的 ASCII 界面图**要加表头行，而图里的对齐靠**显示宽度**（汉字占 2 列）——
   手抄空格必然错位，也用 node 脚本按索引替换整块，并把框加宽 4 列使右边界仍落在显示列 63。

### 验证
`dotnet build -c Debug -p:Platform=x64 -p:OutputPath=<仓库外 tmp>` 跑 **2 次**（初版 / 改光标后），
均 **0 warning / 0 error**（1.82 ~ 3.79 s）；`git ls-files --eol MainForm.cs` = `i/lf w/crlf`（**Edit 未改行尾**）。
巡检脚本**通过**（17 个 md，无结构性问题）。

### 文档
- 新建 `MD文件汇总（AI）/任务&需求/R-20260921-02-图层容器顶部栏与全选.md`（**18 条可判定验收目标**+范围+被否方案）
- `MD文件汇总（AI）/README.md` 索引登记一行
- `待办清单.md` 加 **T31**（🟡 待你操作：实机验收，重点看"置顶二字与 ↑ 图标是否同列"）
  与 **T32**（⬜ 待你决定：半选态要不要保留，不要就删 `OnPaint` 里那一个 `else if`）
- `系统架构&协议设计/系统架构总览.md` 图层模型节补两条（首行是表头 / 全选走重建）
- 根 `README.md`：ASCII 界面图 + §二界面约定第 3 条 + 关键设计决策两条

---

## 第八轮（用户：从稳定版 V5 移植「选点划分 h1/h2/h3」与「合并」）

### 需求原话
> 查看 `D:\MyWork\Gerber\选点软件\稳定版V5\GerberParserV5.0`，有两个功能，本项目由于版本低就没有，
> 分别是选点划分 h1、h2、h3，以及合并功能，切换 h1、h2、h3 放到工具栏里像单选多选那样，
> 在右键菜单中新增 `单选/多选 | 切换到h1[] /切换到h2[] /切换到h3[]`；合并功能放在工具栏中。

### 先去 V5 把语义读清楚（**不能照搬代码**）
V5 是 **Halcon + 单图层**的老架构（`HObject` 缓存 + `Shapes` 类 + 右侧面板），
本工程已去 Halcon 化、多图层化、右侧面板整体删除 —— 所以只搬**语义**，UI 落点按本工程重排。

| 功能 | V5 的做法 | 本工程的做法 |
|---|---|---|
| h1/h2/h3 | `Shapes.HeaderType` 字段 + 右侧 `cboxInsertHeaderSelect` 下拉框 + 三色（red/yellow/green）分别 `DispObj` 缓存 | `SelectedCircle.HeaderType` + **工具栏三个互斥 toggle**（像单选/多选）+ 右键菜单三项；绘制时按类型换色 |
| 合并 | 右侧 `btnMerge` 按钮 + `_isMerge` 模式 + `_mergeShapes` 候选；点第二个时取中点、沿用第一个的形状尺寸生成新 `Shapes` | 工具栏 `合并` toggle + 同样的候选语义 |

V5 的关键细节（照抄语义的部分）：
- 命中处理里 `clickedShape.HeaderType = currentHeaderType` —— **选点即归类**；
- 合并新点 = `((x1+x2)/2, (y1+y2)/2)` + 沿用第一个的形状/尺寸/ID；**原两点保留**；
- 合并模式与"选点"是同一段代码里的两个分支。

### 本工程的落地（改动全在 `MainForm.cs` + `ParamsForm.Designer.cs` 一行文案）
1. **`SelectedCircle.HeaderType`**（属性初始化器 = `"h1"`，持久化）。`HeaderTypes[]` / `NormalizeHeader()`
   / `HeaderColorOf()` 三个小工具把"三个值"收在一处。
2. **h1 的颜色 = `_selectColor`**（参数设置里那个"选中颜色"），h2 = yellow、h3 = green。
   刻意**不照抄 V5 的 h1=red**：老工程里全部点都是 h1，改成红等于"升个版本满屏变色"，
   而用户没要求改配色。要一致只改 `HeaderColorOf` 的 default 分支一行。
   → `ParamsForm` 的标签文案顺带改成「h1 颜色」（否则用户不知道它现在管什么）。
3. **绘制换色只在该变时才 `SetColor`**：控件库 SetColor 要做色名解析，
   逐点调 3852 次/帧纯属浪费；实际数据同类点成片连续，切换次数远小于点数。
4. **写入头类型三处 + 撤销回滚**：单选（仅选中时写）、多选扩散（整批写）、框选（写 + 存进
   `SelectionSnapshot.WasHeaderType`，`Ctrl+Z` 时**与选中状态一起回滚** —— 否则"撤销了一半"）。
5. **合并与单选/多选互斥**：`KMouseDown` 里命中之后、选点分支**之前**插 `if (_isMergeMode) { HandleMergeClick; return; }`。
   否则多选会扩散出一批点，永远凑不出"两个点"。
6. **候选独立于选中状态**：点候选**不改变** `IsSelected`（V5 会顺带选中），改成 gold 轮廓高亮
   （`DrawMergeCandidates`，`SetDraw("margin")`）—— 退出合并模式不留痕迹。
7. 🔴 **合并点必须收编进图形缓存**（本工程与 V5 的**唯一实质差异**，也是相对 V5 的增强）：
   合并点坐标是两点中点，**不落在任何 Gerber 光圈上**，进不了 `_allShapes`；而 `_allShapes` 是
   命中/框选/多选扩散的**唯一依据** → 结果是"画得出来、存得下去、却**怎么点都没反应**"
   （V5 里正是这个状态）。新增 `AppendOrphanSelections()` 把这些游离选点补进缓存，
   并沿用生成它的图形的 `LayerId`（图层深度/隐藏状态与同层图形一致）。
   **顺带修好了另一件事**：以前从 json 恢复的、坐标对不上 Gerber 的选点也是这种幽灵点。
8. **右键菜单新组放在最上方**（`单选`/`多选` + `切换到 h1/h2/h3`，都带勾选）：
   模式在前、动作在后。勾选**必须就地同步** —— 菜单点击后还开着，`ToolStripMenuItem.Checked`
   不会自己变，不写就会出现"点了多选，菜单里还是单选打勾"。
   （不能用 `CheckOnClick`：真实状态在工具栏那份，它会让菜单自己翻转成不一致。）
9. **`BuildAllShapes` 开头清 `_mergeCandidates`**：换工程后候选持有的上一批对象引用会画成"幽灵高亮"。
10. **json v2.4**：每条图形加 `Header`；读端新增 `ReadJsonHeader`（走 `JObject` 索引器，
    缺字段 → `h1`，**不抛异常** —— 理由与 `ReadJsonLayer` 完全一样，只是这次多亏之前踩过）。

### 验证
- `dotnet build -c Debug -p:Platform=x64 -p:OutputPath=<仓库外 tmp>` 共 **4 次**（每轮改动后），
  均 **0 warning / 0 error**（1.63 ~ 2.20 s）
- `git ls-files --eol`：`MainForm.cs` / `ParamsForm.Designer.cs` 均 `w/crlf`（Edit 没改行尾）
- 巡检脚本**通过**（18 个 md）

### 文档
- 新建 `MD文件汇总（AI）/任务&需求/R-20260921-03-选点划分h1h2h3与合并.md`（**22 条可判定验收目标**）
- `数据格式-circles.json.md`：结构示例升 v2.4 + 字段表加 `Header` + 版本演进加 2.4 + 缺字段容错说明
- `MD文件汇总（AI）/README.md` 索引登记；`待办清单.md` 加 **T37**（实机验收）、
  **T38**（合并是保留原点还是替换）、**T39**（下游是否要按 h 分文件导出）
- 根 `README.md`：版本号 v2.4、`SelectedCircle` 字段表加 `HeaderType`、§二工具栏清单、
  关键设计决策两条；ASCII 图的工具栏行用 node 按**显示宽度**重排（框右边界仍对齐第 64 列）

---

## 第九轮（用户指出："你遗漏了一项，h1、…"）

### 遗漏了什么
V5 的 h1/h2/h3 **三个颜色都是可配置的**（`Settings` 有 `HeaderH1Color`/`H2`/`H3`，
`ParamsForm` 里有 `pnlColorPreviewH1/H2/H3` + 三个选择颜色按钮，主窗体读写这套设置）。
第八轮我只做了 **h1（挂在既有的 `SelectedCircleColor` 上）+ h2/h3 写死** —— 等于搬了一半。

**教训：移植一个功能时，先把"这个功能涉及哪些可配置项"在源工程里 grep 一遍**
（`grep HeaderH1Color|HeaderH2Color|HeaderH3Color` 一眼就能看到 `Settings` + `ParamsForm` 两处），
而不是只顺着主流程（字段 → 绘制 → 交互）往下写。

### 补上的改动
| 文件 | 改动 |
|---|---|
| `Properties/Settings.settings` | 新增 `HeaderH2Color`（yellow）/ `HeaderH3Color`（green） |
| `Properties/Settings.Designer.cs` | 两个对应的强类型属性（手写，格式照既有项） |
| `App.config` | `userSettings` 段补同样两项（缺了也能跑，会用 `DefaultSettingValueAttribute`） |
| `ParamsForm.Designer.cs` | 新增 `label2/3` + `pnlColorPreviewH2/3` + `btnSelectColorH2/3` 六个控件，
  y=144/184 两行（行距 40，与 h1 那行同款式）；TabIndex 43~48 |
| `ParamsForm.cs` | `_headerH2Color`/`_headerH3Color` 字段、`SetHeaderColors`、`GetHeaderH2Color`/
  `GetHeaderH3Color`、`PickHeaderColor(header, preview)`（两行共用）+ Load 里刷三个预览 |
| `MainForm.cs` | 两个颜色字段（从 Settings 初始化）、`HeaderColorOf` 改成读设置（**空值兜底** yellow/green）、
  `btnParamSetting_Click` 传三个色 / 回读三个色 / **改完立刻 `RefreshKWindow()`** |

### 两个取舍
1. **h1 的设置项 `SelectedCircleColor` 不改名**（只把界面标签写成「h1 颜色」）：
   用户设置按名字存，改名会让老用户设过的值**静默失效**（界面显示白，其实他设过别的色）。
2. **h1 默认保持白**（不照 V5 的红）：老工程全部点都是 h1，照抄会"一打开满屏白点变红点"。
   默认值的语义必须与"什么都不改"等价 —— 想用红色，用户在参数设置里点一下就行。

### 踩到一次自己的坑
编辑 `ParamsForm.cs` 时，`old_string` 取了整个 `GetBaseTemplatePathFromSettings()` 方法体、
而 `new_string` 只写了签名行 → **方法体被删**（本想在它前面插新方法）。
编译前发现（紧接着的 Edit 修复）。教训与 `2026-09-19` 记的"替换方法签名行要连 `{` 一起取"是同一类：
**`old_string` 要尽量短且唯一，别把"要保留的内容"包进去**。

### 验证
`dotnet build` 又跑 1 次（累计本轮 5 次）→ **0 warning / 0 error**（2.61 s）；
6 个改动文件全部复验 **CRLF + UTF-8 BOM**。

### 文档
`R-20260921-03` 补"参数设置三行颜色"（验收目标 → **23 条**、范围、实现方案表、§6.2 重写、改动清单、
验证记录"重点看四处"）；根 `README.md` 关键设计决策那条改述；`待办清单.md` T37 同步。

---

## 第十轮（用户问："参数设置里的模板文件保存路径还有必要保留吗"）

**只做了代码勘查与判断，未改任何代码**（用户是在问判断题）。

### 勘查结论：`DefaultSavePath` 作用面已压缩到只剩一处
| 触点 | 位置 | 性质 |
|---|---|---|
| `SaveTemplateFile` 无归属分支 | `MainForm.cs:4168` | ✅ **唯一真正决定落点**：新建工程后第一次保存 → `GetBaseTemplatePath()/<工程名>` |
| 另存为对话框初值 | `MainForm.cs:4233` | 仅当 `_currentTemplatePath` 为空，只影响对话框起点 |
| 打开模板对话框初值 | `MainForm.cs:4482` | 仅起点 |
| 首次运行初始化 | `PublicMethod.cs:82-92` | 写成 `exe\template`，与 `GetBaseTemplatePath` 兜底**重复** |
| `SetSavePath(GetCurrentSavePath())` | `MainForm.cs:2099-2101` | **死代码** —— `ParamsForm_Load`（`ParamsForm.cs:32-34`）在 ShowDialog 时用 `GetBaseTemplatePathFromSettings()` 覆盖了文本框与 `_savePath`，主窗体传的值一次都没显示过 |

关键前提：`LoadProjectFromFiles` 把 `_currentTemplatePath` 清空（`MainForm.cs:3950`），
但 `GetProjectName()` 会退化成"第一个图层所在目录名"（`2552`）→ **不为空** → 4168 那条路**可达**。

### 🔴 顺带查出：参数设置点「确定」会静默改写工程归属（`MainForm.cs:2126-2130`）
```
if (!string.IsNullOrEmpty(newPath) && Directory.Exists(newPath))
    _currentTemplatePath = newPath;   // ← 归属被一个"全局默认路径"顶掉
```
`newPath` = 参数窗体文本框内容 = `DefaultSavePath`。后果链：
打开工程 A → 只想去改 h2 颜色 → 点确定 → 归属变成 `DefaultSavePath` → 再点「保存」
→ 写进 `DefaultSavePath\<DefaultSavePath 的目录名>`（通常是 `...\template\template`），A 里什么都没有。
这与 `MainForm.cs:4840-4845` 注释里已修掉的 `LoadSavedPath` 那条是**同类 bug 的另一个入口**——
那条修了，这条还在。**与"设置项留不留"无关，无论如何该删这 5 行。**

### 用户裁定：方案 B —— 删掉设置项（已实施）

3 个方案给出去，用户选 **B**：删掉设置项 + 相关代码，无归属时弹目录选择器当场选落点。

| 文件 | 改动 |
|---|---|
| `MainForm.cs` | 删 `GetBaseTemplatePath` / `GetCurrentSavePath` / `UpdateSavePath` / `LoadSavedPath` 及调用点（构造函数那行 + `btnParamSetting_Click` 里那几行）；`SaveTemplateFile` 无归属分支改弹 `FolderPicker`；`SaveTemplateAs` / `LoadTemplateFile` 初值改用新方法；新增 `GetProjectParentBrowseDir()` + `GetParentDirIfExists()` |
| `ParamsForm.cs` | 删 `_savePath` / `SetSavePath` / `GetSavePath` / `btnBrowse_Click` / `GetBaseTemplatePathFromSettings`；`btnOk_Click` 简化为"置 DialogResult"；删 `using System.IO` |
| `ParamsForm.Designer.cs` | 删 `label8` / `txtSavePath` / `btnBrowse`；三行颜色与「确定」各上移 40 px；`ClientSize` 428 → 388（**宽 874 不动**） |
| `PublicMethod.cs` | 删 `InitializeSettings()`（-19 行，全工程无调用点的死代码） |
| `Settings.settings` / `Settings.Designer.cs` / `App.config` | 删 `DefaultSavePath` |

`GetProjectParentBrowseDir()` 四级优先级：当前工程目录的父目录 → 图层所在目录的**父目录**
→ 上次打开的工程目录 → 桌面。每级都要求父目录**实际存在**才采用（`GetParentDirIfExists`），
否则系统对话框会自己退化到"我的电脑"，比给桌面更糟。

⚠ **为什么必须是"父目录"**：对话框返回值会再拼工程名（`Path.Combine(selected, folderName)`），
初值若给"图层所在目录自己"，用户直接确认就得到 `…\1516601-00-C\1516601-00-C` —— 白套一层。

**顺带确认**：根 README 第 205 行早就写着"此时还没有工程目录（**保存时才问存哪**）"——
本轮实现正好把这句话坐实了，说明这不是新语义，而是把没兑现的语义兑现。

### 验证
- `dotnet build -c Debug -p:Platform=x64 -p:OutputPath=<仓库外 tmp>` → **0 warning / 0 error**（0.58 s）
- `grep "DefaultSavePath|txtSavePath|btnBrowse|SetSavePath|GetSavePath|InitializeSettings|GetBaseTemplatePath"`
  → **No matches**（源码零残留）
- 7 个改动文件 `git ls-files --eol` 全部 `i/lf w/crlf`；首三字节均 `239,187,191`（BOM 保留）
- `ParamsForm.Designer.cs` 因删控件连带要收拾布局，走了**整文件重写**（Write → LF），
  与 5 个被改过的 md（2 新建 + 3 更新）一起用 node 脚本归一为 CRLF
  （脚本打印前后 `crlf/lf/bom`，确认：两个新 md 0→209 / 0→129 行 CRLF，其余本来就是 CRLF、内容未动；
  全部**无 BOM**，与 `MD文件汇总（AI）` 下的约定一致）

### 文档
- 新建 `MD文件汇总（AI）/任务&需求/R-20260921-04-移除模板保存路径设置.md`（**20 条可判定验收目标** + 范围 + 5 条被否方案）
- 新建 `MD文件汇总（AI）/问题&解决方案/参数设置确定按钮改写工程归属.md`
  （根因三层 + 方案 + 5 条备选对照 + **4 条教训**：顺带写全局状态 / 检查赋值是否真生效 / 同类缺陷找全 / "看起来正常的提示"更危险）
- `待办清单.md` 加 **T40**（实机验收，重点看四条）；`MD文件汇总（AI）/README.md` 索引登记两行
- `系统架构&协议设计/系统架构总览.md`：代码地图行数刷为实测（MainForm **4956** / ParamsForm **224 + 187** /
  PublicMethod **79**，原来写的是 4172 / 220+149 / 98）、核对日期更新、
  **关键不变量加第 12 条**（保存落点只有两个来源）
- 根 `README.md`：关键设计决策补"落点只有两个来源"一条（含"必须是父目录"的理由）
- 巡检脚本**通过**（20 个 md，无结构性问题）

### 待定（留给用户）
`../GerberParserV4.0_交接文档.html`（**仓库外**）第五节讨论过"有归属就地保存"那条设计决策。
本轮把它改成了"有归属就地 / 无归属当场问"，**尚未同步那份 HTML** —— 等用户决定要不要一并更新。

---

## 第十一轮（用户：插针机导入本项目 json 报错，崩在 json_value.cpp）

### 用户描述
> 本项目导出的 json 与老项目对比改动很大，导致 `RivetPin2019` 解析 json 报错，报错在 `json_value.cpp`；
> `Common` 是所有代码文件，`CADImportDlg` 是解析 json 的入口。
> 最后要怎么改？是改插针机协议，还是改本项目？

### 结论：不是 jsoncpp 的 bug，是两份 json 的 `Shapes` 顶层结构不同

| | 老项目（稳定版 V5） | 本项目（高清版 V4，v2.4） |
|---|---|---|
| 写端 | `…\选点软件\稳定版V5\GerberParserV5.0\MainForm.cs:1636-1713` | `MainForm.cs:4415-4449` |
| `Shapes` | `{ h1:{Circles,Rectangles,Ovals}, h2:…, h3:… }` —— 头类型是**键** | `{ Circles:[…], Rectangles:[…], Ovals:[…] }` —— 头类型在每条记录的 `Header` 字段 |

本项目 v2.0 起就是按形状分组（那时还没有 h1/h2/h3），**插针机从来没成功读过本项目的 json 一次**
—— 这是两条线第一次对接才暴露的，不是"某次升级弄坏了"。

### 崩溃链路（逐跳核实过源码）

`Common\CADImportDlg.cpp:435` `shapesByHeader.getMemberNames()` → 拿到 `["Circles","Rectangles","Ovals"]`
→ `:442` `headerGroup.isMember("Circles")`，而 `headerGroup` 是**数组**
→ jsoncpp `isMember`（`json_value.cpp:1335`）不判类型直调 `find`
→ `json_value.cpp:1117` 断言 `type()==nullValue||type()==objectValue`
→ `assertions.h:54` `JSON_ASSERT_MESSAGE` → `JSON_FAIL_MESSAGE` → `Json::throwLogicError`
（`config.h:20` `JSON_USE_EXCEPTION=1`，是**抛异常**不是 abort）
→ `ParseJSONContent` 无 try/catch → 未捕获异常 → 崩，抛出点定位在 `json_value.cpp`。

### 🔴 顺带认定的真 bug（与格式无关）
`isMember` / `operator[]` / `asCString` 前**一律没判类型**，且整个 `ParseJSONContent` 无 `catch` ——
任何畸形 json 都会让**产线程序**崩。不管最后选哪条路，这三处都得修。

### 建议：改插针机（双格式兼容），不动本项目

判定依据：
1. `Common\` 全目录 grep `TemplateInfo|Shapes|circles.json` → **只有 `CADImportDlg` 一个消费点**。
2. V4 扁平格式是 V5 分组格式的**超集**：V5 能表达的"一个点 + 一个头类型"，V4 一条记录就够；
   反过来 V5 格式**装不下 `Layer`**（多图层是本项目刚落地的核心能力）。让超集退回子集 = 锁死上游表达力。
3. 消费方该对上游宽容（Postel）；纯加分支，老 V5 模板照样能读。
4. 反过来改本项目 = `Header` 重新提升为结构键，与"Layer/Header 正交"的设计打架，
   且 V4 自己的读端要跟着改。

**没动手改插针机代码** —— 那是另一个仓库、产线设备、共享文件（含 R9/RS/R5/R6 分支），等用户拍板。
只给了可直接落地的代码（共用的 `ParseShapeArray` + 按 `Shapes` 顶层键分派 + 三处健壮性加固）。

### 新积累的环境事实（跨会话有用）

- `Common\CADImportDlg.cpp` 是 **UTF-8 BOM + CRLF**（实测 578 CRLF / 0 裸 LF），
  与 `Common\` 里绝大多数文件（GBK 无 BOM，如 `CModelRes.cpp` 前 4 字节就是 `2F 2F 20 43`）
  **不同** —— 按 GBK 读写这个文件会直接乱码。
- `RBLWorkMin` **不是 git 仓库**（`git rev-parse` 返回空），查不了改动历史，只能看 `LastWriteTime`
  （`CADImportDlg.cpp` = 2026-08-18）。
- `RBLWorkMin\PROJECT_CONTEXT.md` 是插针机工程的交接文档（"只改 RS 系列"铁律 + jsoncpp 1.9.7
  移植记录 + 各轮改动），跨会话接手必读。
- 插针机的 jsoncpp 源码：`RBLWorkMin\CombineMySource\Include\Json\*` + `Src\Json\*.cpp`（源码编译，非外部 lib）。
  ⚠ `D:\MyWork\Gerber\v1\` / `v2\` 下也有同名 `json_value.cpp`，**不是**它用的那份。

### 产出文档

- 新建 `MD文件汇总（AI）/问题&解决方案/插针机导入json崩溃（Shapes顶层按形状分组）.md`
  （根因链路 + 双格式兼容代码 + 四种方案取舍表 + 两个待拍板语义问题 + 4 条验收目标 + 编码/行尾警告）
- `MD文件汇总（AI）/系统架构&协议设计/数据格式-circles.json.md`：
  新增第七节「下游：插针机」（**已核实**它读哪些字段、不读哪些；含两张结构对照图），
  「待核实」里"下游实际读取了哪些字段"一条**就此销账**；补一句"两条线第一次对接"
- `MD文件汇总（AI）/待办/待办清单.md`：加 **T41**（定夺改哪边）、**T42**（隐藏图层的点要不要导出）
- `MD文件汇总（AI）/README.md` 索引登记一行
- 行尾：新文件 Write 出来是 LF → node 归一为 CRLF（228 行）；另 3 个被 Edit 的文件**未被改成 LF**（复验 CRLF 完好）
- 巡检 `检查文档归置.ps1` **通过**（21 个 md）

---

## 第十二轮（用户两个决定：改插针机不做 V5 兼容 + 坐标扁平化 v2.5）

### 用户原话
> 改插针机，但不要旧格式兼容，V5 后续将废弃
> 还有，我建议把 `Positions: []` 直接改为 `X`、`Y`

### 改动 A：插针机 `CADImportDlg`（`D:\MyWork\Account\1_BEST\RBLWorkMin\Rayble\Common\`）
- `.h`：加 `ParseJSONContentImpl` 声明
- `.cpp`：
  - 新增文件级 `ParseShapeArray`（`:372-441`）：**一个记录一个点**，坐标读记录上的 `X`/`Y`
    （另留 `Positions[0]` 兼容分支），头类型读 `Header`（缺失 / 空串 / 陌生值 → `h1`，与上游口径一致）
  - `ParseJSONContent` 拆成**薄壳 + `ParseJSONContentImpl`**：薄壳兜 `Json::Exception` /
    `std::exception` / `...`（原函数体整体改名，不必给整个函数体重新缩进）
  - 形状解析改为 `ParseShapeArray(shapes["Circles"], …)` 三行；**整段删掉按头分组的循环**
  - 补类型判断：`root.isObject()` / `templateInfo.isObject()` / `HeaderColors.isObject()` /
    `TotalCount.isNumeric()`
  - 统计改为按形状 + 按头类型分别计数（消息里显示 h1/h2/h3）
- **一处未动**：`AddShapesToModel` / `CountShapesByHeaderType` / 各机型的 `#if` 分支（R9/RS/R5/R6）
- ⚠ 插针机**不在本机工程里，无法编译**，只能静态复核（括号 / 分支 / 类型都逐个看过）

### 改动 B：本项目 json **v2.5**（坐标从 `Positions[]` 提到记录上）
- 写端 `MainForm.cs`：三条记录（圆 / 矩形 / 椭圆）`Positions = new[]{ new{ X, Y } }`
  → `X = Math.Round(…,4), Y = Math.Round(…,4)`；`Version` `2.4` → `2.5`
- 读端：新增 `ReadJsonPosition`（**新格式优先、`Positions[0]` 兜底**）+ `TryReadDouble`；
  三处 `(double)xxx.Positions[0].X` 改为调它
- 🔴 **读端为什么必须兼容**：用户已有的 v2.0~2.4 工程是**数据**，不能因为改格式就打不开。
  这与"插针机不做 V5 兼容"不矛盾 —— 那边是另一个产品线的旧格式，这边是自己的历史存档

### 验证
- `dotnet build -c Debug -p:Platform=x64 -p:OutputPath=<仓库外 tmp>` → **0 warning / 0 error**（4.17 s）
- 行尾 / 编码复验（node 脚本，非 PowerPoint）：
  - `CADImportDlg.cpp` BOM ✓ / CRLF **633** / 裸 LF **0**
  - `CADImportDlg.h` BOM ✓ / CRLF 80 / 裸 LF 0
  - `MainForm.cs` BOM ✓ / CRLF **5018** / 裸 LF 0
  - 5 个 md 全部 **CRLF、无 BOM**（符合工程内约定）
- 巡查脚本 **通过**（21 个 md，无结构性问题）
- ⚠ 本轮 **Edit 工具一次都没把文件改成 LF**（三次跨仓库改写都保住了 CRLF）——
  但仍按惯例逐个复验过，没有靠"上次没事"推断

### 文档
- `系统架构&协议设计/数据格式-circles.json.md`：结构示例升 v2.5（坐标在记录上）、字段表、
  版本演进加 2.5、**第七节「下游：插针机」整节重写**（已改 + 明确不兼容 V5 + 格式对照图更新）、
  待核实清单里 `Positions` 那条销账
- `问题&解决方案/插针机导入json崩溃（Shapes顶层按形状分组）.md`：开头加"**状态 ✅ 已修**"，
  第三章标题改「已实施」，**新增 3.3「实际落地的改动」**（含 `ParseShapeArray` 全文；
  原 3.2 的双格式兼容稿**保留但标注"未采纳"**，作为分析过程留档），第四章方案 A 结论改「已采纳」
  并注明"不要 V5 兼容"，第六章验收目标重写为 5 条
- `待办/待办清单.md`：T41 → 🟡 **待验收**（改插针机已定夺，附已改内容与"未编译"警告）；
  新增 **T43**（v2.5 待实机验证，三条重点）
- `系统架构&协议设计/系统架构总览.md`：核对日期行的行数 4956 → **5018**；
  **关键不变量加第 13 条**（json 格式演进：写端一份真相 / 读端兼容旧格式 / 禁用 `dynamic`
  读可能不存在的字段）
- 根 `README.md` §1.6：`Positions` 那条改写为"坐标直接挂在记录上（v2.5）"；版本号 2.4 → 2.5；
  指明 `LoadJsonFormat` 里**其余字段仍是 `dynamic`**（T09 仍在）

### 留给用户决定
- 插针机需在**产线工程**编译后按 5 条验收目标实机验证（T41）
- 插针机里 `Positions` 那个兼容分支要不要**彻底删掉**（若现场确认没有 v2.4 老 json）

---

## 第十三轮（用户追加裁定：连读端也断掉，不需要兼容）

### 用户原话
> 确实要连读端也断掉，删 ReadJsonPosition 里那段 else 就行（注释里标了位置）。不需要兼容

### 做了什么
- `MainForm.cs` 的 `ReadJsonPosition`：**删掉 `Positions[0]` 的整个 else 分支**，现在只读记录上的
  `X` / `Y`；方法注释改为"只认 v2.5 格式"，并 🔴 标明"打开老工程坐标全读成 0 是**有意为之**"
- 写端版本注释 + 一处调用行注释同步改
- **插针机 `ParseShapeArray` 里的同款 else 分支一并删掉** —— 用户说的"读端"指本项目，
  但插针机那段兼容分支是我上一轮刚加的，留着就自相矛盾（他上一条已说"不需要兼容"）
- 全线 `Positions` 残留复核：两个源文件里**只剩注释**提及（说明不兼容），无任何解析代码

### 验证
- `dotnet build -c Debug -p:Platform=x64 -p:OutputPath=<仓库外 tmp>` 再跑 **2 次**
  （删分支后 / 注释修正后）→ 均 **0 warning / 0 error**（8.33 s / 7.59 s）
- 行尾：`MainForm.cs` BOM ✓ / CRLF **5006** / 裸 LF 0；`CADImportDlg.cpp` BOM ✓ / CRLF **623** / 裸 LF 0
- 巡查脚本 **通过**（21 个 md）

### ⚠ 已在 5 处文档 + 2 处代码注释里写明后果
打开 **v2.0~2.4 的老工程** → 坐标全部读成 0、点堆在原点（**属预期，不是缺陷**）；
若再点保存，就把"全 0"写回去，**不可逆** —— 恢复只能靠 git 历史或备份。
覆盖位置：`数据格式-circles.json.md`（字段表 + 版本演进 + 第五节 + 第七节）、
`问题&解决方案/插针机导入json崩溃（Shapes顶层按形状分组）.md`（3.3 说明 + 验收目标第 5 条）、
`待办清单.md` T43①、`系统架构总览.md` 不变量第 13 条、根 `README.md` §1.6。

### 文档
- `系统架构总览.md`：**不变量第 13 条重写** —— 从"读端兼容旧格式"改为
  **"容错 ≠ 兼容旧格式"**（字段缺失仍容错：`Layer`→空串、`Header`→`h1`；格式的旧版本有意断掉）；
  核对行数 5018 → **5006**
- `数据格式-circles.json.md`：字段表 / 版本演进 2.5 / 第五节容错段 / 第七节 —— 四处改为"不兼容"
- `待办清单.md` T43① 改为"旧工程已不兼容，属预期；**别对老工程点保存**"
- 根 `README.md` §1.6 同步

---

## 第十四轮（工程管理三改 + 拖放 + 命中排序两修）

### 用户四个需求
1. 新建工程要能**自定义工程名**（原来只能从"首个 Gerber 所在目录名"推）
2. 支持**拖文件进窗口**：有工程 → 加图层；没有 → 新建
3. 「打开工程」没有**默认工作路径**，每次都要从上次那层退一层再进另一个
4. 在 `MD文件汇总（AI）/` 下建**更新日志**并登记进 README

### 落地
- 新增 `SaveProjectDialog.cs`（工程名 + 保存位置 + 落点实时预览 + 已存在确认）→ 接入「首次保存」与「另存到」；
  顺带让"另存到同一父目录 + 改名"成为**工程改名的正规入口**
- 新增 `ProjectNameDialog.cs`（只问名字）→「新建工程」改为 **先名字 → 选文件 → 载入完自动落盘**
- **拖放**：`MainForm` + `kWindowControl1` **双挂** `AllowDrop`（鼠标落点几乎总在画布上）；
  `OnFileDragDrop` 按 `_layers.Count` 分派；新增 `AppendLayersFromFiles`
  （🔴 **不 ResetProjectState / 不动归属 / 不重置视图**）+ `ReplaceLayer`（同名弹窗询问，
  替换时**清该层选点 + 剔撤销栈快照**）；抽 `TryParseLayerFile` 供两条路共用
- 保存图层拷贝的判据从"目标是否存在"改为 `IsSamePath`（否则"拖入替换"保存后等于没发生过）
- 新增设置项 `DefaultProjectRootPath`（参数设置**最上面一行**）→ `GetProjectParentBrowseDir` 第⓪级；
  🔴 **只做对话框初值，绝不参与落点决策** —— 旧 `DefaultSavePath` 的缺陷正是越界顶替 `_currentTemplatePath`
- 新建 `MD文件汇总（AI）/更新日志.md`

### 🔴 顺带查出并修掉两个真 bug（用户报的）
1. **只有一个图层也总是选到大圆**：同层内只有"边界距离"一档，而圆内部 `d = len − r` → 大圆通吃。
   修：同层内改为"命中内部优先 → 都在内部时**尺寸小者胜**"（新增 `AreaProxy`）。
   脚本对照验证：**只有"命中多个内部图形"时结论变**，其余四种场景行为一字未动。
2. **置顶了小圆图层仍选到大圆**（用户明确指出"改动之后才有"）：
   `FinishProjectLoad` 里 `BuildAllShapes()` **早于**图层 Id 分配 → 图形的 `LayerId` 全被写成 0 →
   `LayerDepth` 全为 −1 → "叠放优先"这一档**静默失效**。
   **为什么"以前没有"**：打开**存过选点**的工程会因"恢复选点"再跑一次 `BuildAllShapes`
   （那时 Id 已分配）而侥幸正常；**新建工程没有第二次** → 必然中招。
   → 是本轮新增的"新建完自动保存"把它暴露的，**不是新引入的缺陷**。
   修：抽 `EnsureLayerIdentity(depthOf)`，在 `BuildAllShapes()` **之前**调
   （`FinishProjectLoad` / `AppendLayersFromFiles` 两处）；
   `BuildAllShapes` 末尾加**自检日志**（`LayerId <= 0` 的图形数，正常应为 0）。

### 验证
- `dotnet build` 共 **7 次**（含一次**故意失败**：改了调用点还没写实现 → `CS0103`，补上后通过），
  最终 **0 warning / 0 error**
- 行尾/编码复验：9 个源文件 BOM + CRLF、无裸 LF；`ShapeQuery.cs` 本次**未被改成 LF**
- 沙盒限制：`Reflection.Assembly.LoadFrom` 被安全策略拦（与 `Add-Type` 同类）→
  反编译路线改用 dotnet + ICSharpCode.Decompiler（未跑完，bug 已先行定位）

### 教训
- 写入方依赖的前置条件（Id 已分配）**不能靠"另一个方法碰巧在我之前跑过"来满足** ——
  跨方法的隐式顺序依赖要么抽成显式的第一步，要么写进断言/自检。
- 排序键设计要问一句"**这个键在退化场景（只有一个图层）下还有意义吗**"，
  否则修好的只是用户当时报的那个子场景。

### 文档
- 新建 `MD文件汇总（AI）/更新日志.md`（用户要求，12 条按主题汇总）
- `问题&解决方案/选点只能选到最大圆圈那一层.md`：补 §5.4（同层排序）+ §5.5（身份分配晚了半拍 + 教训）
- `数据格式-circles.json.md`、`待办清单.md`、根 `README.md` 同步 v2.5 与"读端不兼容旧格式"
- ✅ 随后补完：新建 `R-20260921-05-工程管理与拖放加载.md`（约 35 条验收目标 + 范围 + 6 条被否方案）；
  `MD文件汇总（AI）/README.md` 索引登记（更新日志 + R-05 + 问题节两处描述更新）；
  `待办清单.md` 加 **T44**（工程管理实机验收）；根 `README.md` 关键设计决策补 3 条
  （默认工作路径的边界 / 新建完自动落盘 / 拖放）+ 命中排序与图层身份两条改写；
  `系统架构总览.md` 行数刷新 + 不变量加第 14、15 条；
  巡查脚本**通过**（23 个 md，3 条"引用未从仓库根写全"的提示已修）

---

## 第十五轮（默认工作路径补默认值）

### 用户要求
> 默认工作路径应该有个默认值：`D:\MyWork\Gerber\选点软件\高清版V4\GerberParserV4.0\bin\x64\Debug\Broad`

### 做法
那个路径 = **exe 旁边的 `Broad` 文件夹**（exe 在 `bin\x64\Debug\`），所以：

- 设置项 `DefaultProjectRootPath` 的默认值填 **`Broad`（相对路径）**，**不是绝对路径** ——
  ⚠ 绝对路径写进默认值，换台机器就指向不存在的地方。
  **这是 `Program.FileName` 当年的原样错误**（MEMORY 第二节有记录），不能重犯。
- 新增 `MainForm.GetDefaultProjectRoot()` 负责解析：
  空 → `程序目录\Broad`；相对路径 → 拼程序目录；绝对路径 → 原样用，
  **但它若不存在（换了机器 / 被删了）就回退到 `程序目录\Broad`**（不死抱一个不存在的路径）
- 新增 `EnsureDefaultProjectRoot()`：启动时保证目录存在（失败**只记 KLog、不弹框**）
  → `MainForm_Load` 调用。**不建它的后果**：目录不存在时"新建工程自动落盘"会退化成弹框问位置，
  等于默认值白设。
- 四处接入改用它：`GetProjectParentBrowseDir` ⓪ / `OnOpenProjectClick` / `AutoSaveNewProject` /
  `btnParamSetting_Click`（参数设置里显示**解析后的绝对路径**，人看得懂）
- **语义变化**：清空该项 = **回到默认值**（程序目录\Broad），不再是"不设置"

### 验证
- `dotnet build` → **0 warning / 0 error**；行尾/编码复验全部 OK

### 文档
- `R-20260921-05` 的 C 节加 **C0**（开箱即有默认值）、改 **C4**（清空 = 回默认值）
- `更新日志.md` 第 5 条补"开箱即有默认值 + 为什么要用相对路径"
- 根 `README.md` 的 `GetProjectParentBrowseDir` 优先级描述同步

---

## 第十六轮（用户裁定：不同图层同坐标的点"不被允许"）

### 用户原话
> 当用户保存时，出现了不同图层、同坐标的点是两个点，这种情况是不被允许的，
> 要么改模型，要么在用户保存时弹窗警告

这条等于给 **T13**（"下游能否接受同坐标多条"）画了句号：**不接受**。

### 先查清"两个点"从哪来（单选不会）
`ShapeQuery.HitTest` 只返回一个图形（按图层叠放取胜者），所以单选永远只点到最上面那条。
真正的来源只有三条：

| 路径 | 位置 | 为什么没去重 |
|---|---|---|
| **框选**（主因） | `MainForm.cs:3453-3469` | 判"是否已选"用四维 `ShapeKey`，跨图层同坐标是两个不同的键 → 两条都进 `_selectedCircles` |
| 隐藏上层 → 点下层 → 恢复上层 | 规则 R1「隐藏 = 数据保留」 | 看不见的旧点还在数据里，用户以为没点过 |
| 合并点 / 游离点落在已有坐标 | `MainForm.cs:1267-1277` | `CreateMergedShape` 取中点，不查全局坐标 |

而保存端 `SaveTemplateToPath` 落盘前**零查重**。

### 关键判断：这是**两套口径**，不是一套
- **图形唯一键 `ShapeKey`（四维）**：管"选点对象 ↔ Gerber 图形"的一一对应，**不能动**
  （少了图层维度会静默退回纯距离排序 = 刚修完的"只能选到大圆"）。
- **交付给插针机的落点集合**：下游按**坐标**定位、不读 `Layer`，所以口径**只认坐标**。

→ "改模型"并不需要动四维键；但最终**没走改模型这条路**（理由见下）。

### 用户选定：保存前检出 + 弹窗（不是自动去重、不是改选点行为）
- 新增 `DuplicatePositionDialog.cs`：纯展示（明细由 MainForm 拼好传入），
  按钮「返回修改」/「仍然保存」，**回车与 Esc 都落在「返回修改」**（默认动作必须是不写盘那个）
- `MainForm.cs` 新增四个方法（`FindDuplicatePositionGroups` / `DescribeShapeSize` /
  `BuildDuplicateDetailText` / `ConfirmNoDuplicatePositions`），调用点 `SaveTemplateToPath` **开头**
  （在"是否覆盖"确认之前）→ 返回 false 时**一个文件都不写**，工程归属也不更新
- 明细每组带「图层 / 形状尺寸 / 头类型」，另标 `⚠ 该图层当前已隐藏`、`⚠ 当前未选中`
  （这两条容易被忽略：隐藏层的点照样导出；`IsSelected=false` 的点也会被写出）
- 明细显示上限 50 处（`DuplicateDetailMaxGroups`），超出的只进日志；日志用 `WARN` 写全量

### 为什么不去重、也不在选点端拦
1. **保留哪一条程序猜不准**：GBL / GBS 常在同坐标放不同尺寸的图形（焊盘与外形框），
   插针机要打的通常是其中某一条；按"最上层 / 尺寸最大"猜反 = **打错针位**。
2. **选点端有五个入口**（单选 / 多选扩散 / 框选 / 合并 / 游离收编），逐个设防必漏；
   出口是唯一写盘点（新建自动落盘 / 就地保存 / 另存为都汇到 `SaveTemplateToPath`）。
3. **隐藏图层悖论**：藏起来的旧点算不算存在？算 → 用户莫名被拒；不算 → 重复照样产生。
   这个悖论只有落到出口才消解。
4. 出口还能覆盖**老工程里已经存下的**历史重复点。

### 实测（不是推断）
在 `C:\Users\59538\.workbuddy\tmp\zerotest`（`dotnet new console` + `TargetFramework=net48`）
把判重逻辑原样复刻后跑：

- **运行时先确认**：`Environment.Version = 4.0.30319.42000` / `.NET Framework 4.8.9345.0`
  （否则结论不成立 —— 一开始跑在 net9 上，哈希行为可能不同）
- 12 个点（跨图层同坐标 / 同层同坐标不同尺寸 / 舍入后相等 / 负零 / 3 个不重复点 / 无图层合并点）
  → 检出 **4 处 / 9 个点**，与预期一致；组内**最上层在前**、组间按坐标升序；明细格式符合预期
- 🔴 **负零那条：我的推断错了**。原本以为 `(-0.0).Equals(0.0)` 为 true 但 `GetHashCode()` 不同
  （符号位参与哈希）→ 字典分桶 → 漏判，还写了归一函数 `NormalizeCoordinateKey`。
  实测**推翻**：net48 下两者 `GetHashCode()` **都是 0**、可互相命中字典。
  → **删掉那个多余的归一函数**，只把实测结论写进注释（免得以后有人再怀疑一遍）。
- **`Math.Round` 银行家舍入**这个边界是自测时暴露的：`Math.Round(35.12345,4)=35.1234` 而
  `35.12346→35.1235`，两个只差 `1e-5` 的邻居会落到不同的第 4 位小数 → **不判重复**。
  这不是本次引入的（**落盘时也是这么舍入的**，所以文件里本来就是两个坐标），
  已记为已知边界（要覆盖需另立"坐标容差"口径，本次没做）。

### 验证
- `dotnet build -c Debug -p:Platform=x64 -p:OutputPath=<仓库外 tmp>` 共 **3 次**，均 **0 warning / 0 error**
- `git ls-files --eol`：`MainForm.cs` / `.csproj` 均 `w/crlf`；新文件 `DuplicatePositionDialog.cs` CRLF + BOM
- ⚠ **csproj 我一度给加了 BOM**（归一脚本统一按"加 BOM"处理），而 HEAD 里该文件**没有 BOM** →
  单看 diff 会多出一行 `<?xml` 差异。已用脚本去掉，现在 csproj 只剩 3 行新增（我的 1 行 + 上轮 2 行）

### 文档
- 新建 `MD文件汇总（AI）/任务&需求/R-20260921-06-保存前同坐标重复点检查.md`
  （8 条可判定验收目标 + 范围"不包含什么" + 6 条被否方案 + 两个边界实测记录）
- `待办清单.md`：**T13 → ✅ 已裁定**（原"待你确认 / 联系下游"销账）；新增 **T45**（实机验收）
- `README.md`（仓库根）：**§1.4 的"为什么不做导出端按坐标去重"整段推翻重写**、
  §五-1 表格行改为"两套口径并存"
- `数据格式-circles.json.md`：§三 改口（"重复坐标是正常的"→"可能，但保存时会被拦"）、
  新增 **§四之二「导出端的第二条口径」**
- `问题&解决方案/重复坐标的图形互相顶替.md`：末尾加"本节结论的适用范围被收窄了"的补充块
  （模型内仍是各占一条，保存时会被告知冲突）
- `更新日志.md` 加第 13 条；`MD文件汇总（AI）/README.md` 索引登记一行
- 巡检脚本**通过**（24 个 md，无结构性问题）

---

## 第十七轮（用户提议：不同缩放比例下绘制精度动态变化）

### 用户原话
> 当图层、图形庞大时，显示几万个图元，会导致缩放卡顿，我建议不同缩放比例下绘制精度可以动态变化，
> 例如默认缩放视图下绘制精度最低，越放大越精细；

用户随后提供了真实的大数据量工程：
`C:\Users\59538\Desktop\gerber文件\Gerber(萨玛瑞)\...\q02578f044a00`（8 个 Gerber 文件）。

### 先用真实数据把量级钉死（`lodstat` 临时工具，只读）
| 项 | 值 |
|---|---|
| 规模 | 8 文件 / **20188 个图元** / 光圈组 29 |
| 板子 | **221.2 × 133.2**，默认视图 scale = **4.88 px/单位** |
| 屏幕尺寸分布 | 2-3px **19.1%** / 3-5px **77.6%** / 5-10px 2.9% / 10-20px 0.5% / **<2px = 0%** |

🔴 **反直觉结论：这块板上没有"亚像素图元"，最小的也有 2.2 px。**
物理原因：PCB 图形有工艺尺寸下限（焊盘/过孔/线宽 ≥0.2~0.3mm），
在整板视图下 0.3mm × 4.88 = 1.46px…… 实测最小 2.2px 说明这批图形的最小尺寸约 0.45mm。
→ 所以**"把亚像素图形跳过"这条路能省 0%**。它是"对的做法"，但**不是解药**。
（用户最初设想的主要形式就是这个，必须如实说明。）

### 再测"单次绘制成本"，找到真正的解药（`gdiperf` 临时工具）
离屏 GDI+、抗锯齿开、Release、3 轮中位、每种 20000 个：

| 屏幕直径 | 描边圆 | 填充圆 | **同尺寸方块** | 方块/描边 |
|---|---|---|---|---|
| 1px | 1.73μs | 1.04 | 0.50 | **0.29x** |
| 3px | 2.42 | 1.43 | 0.65 | **0.27x** |
| 6px | 3.80 | 2.01 | 0.87 | **0.23x** |
| 12px | 4.49 | 2.55 | 1.43 | **0.32x** |

- 🔴 **合并成一条 `GraphicsPath` 再画 → 反而慢 3.5 倍**（20000 个椭圆 `DrawPath` 562ms
  vs 逐个描边 162ms；`FillPath` 也有 1.89x）。GDI+ 的路径 figure 一多，内部开销**超线性**。
  —— 这条"看起来最诱人的批量优化"被实测否掉，是这个会话最有价值的发现之一。
- 按该板真实分布、**按每个光圈的实际屏幕尺寸插值累加**：**现状 56 ms/帧 → "<5px 用同尺寸方块"后 16.5 ms/帧（0.30x）**。
  其中 **3~5px 那一档（15660 个）独占约 32 ms（57%）**，是成本大头 —— 所以阈值必须覆盖到它。
  ⚠ **口径更正**：最初报的 `76ms / 18ms` 是按"每个尺寸档取一个代表值"粗算的，
  `56ms / 16.5ms`（按实际屏幕尺寸插值）更准，**以它为准**。

### 实现（`MainForm.DrawGerberApertures`）
| 屏幕尺寸 | 画法 |
|---|---|
| < 1.0 px | 不画 |
| 1.0 ~ 5.0 px | **同尺寸实心方块**（边长 = 图形自身外接框 → **视觉尺寸不变**，只抹掉"圆/椭圆"） |
| ≥ 5.0 px | 精确绘制（描边圆/矩形/椭圆，与原实现一致） |

四个要点：
1. **判据用"屏幕尺寸"而不是"缩放级别"** —— 屏幕尺寸 = 世界尺寸 × scale，天然等价于
   "越缩小越粗、越放大越精细"，放大后自动恢复精确档，**不必维护"当前第几档"状态**，
   也就没有切档时的画面跳变。阈值就是 `LodSkipPx` / `LodBlockPx` 两个常量。
2. **方块边长用图形自身外接框**（不是固定 2×2）。第一版我写成固定 2×2 px，
   自查时发现**会把 6px 的图形缩成 2px —— 尺寸缩了 3 倍**，视觉上底图会变稀。
   改成同尺寸后尺寸完全不变。**这个错误是在"先算一遍预期收益"时自己抓到的。**
3. **只降级底图**（Gerber 光圈）。选点 `_selectedCircles` 永远精确绘制 ——
   "看不清"与"选不了"是两回事，后者是缺陷。
4. **每图层内分两趟画**（pass 0 精确 margin / pass 1 简化 fill），把 `SetDraw` 切换降到每层 1 次。
   🔴 两趟**必须放在图层循环内部**：挪到外面会让上层的简化图形盖住下层的精确图形，
   直接把图层叠放顺序搞乱（这个错很隐蔽，代码看着还挺"干净"）。

### 验证
- `dotnet build -c Debug -p:Platform=x64 -p:OutputPath=<仓库外 tmp>` → **0 warning / 0 error**（2.00 s）
- 两个临时工具都在 `C:\Users\59538\.workbuddy\tmp\`（`lodstat` / `gdiperf`），**没进仓库**
- 只读用户桌面的 Gerber 目录与 `bin\x64\Debug\` 下的文件，**未写任何工程文件**

### 阈值为什么是 1px / 5px（用户追问"为什么选1，5px"）
把"阈值 → 收益"曲线打出来找拐点（同一块板、按实际屏幕尺寸插值）：

| 阈值px | 降级占比 | ms/帧 | 相对现状 | 本档再省 |
|---|---|---|---|---|
| 3.0 | 19.1% | 48.8 | 0.87x | 12.7% |
| 4.0 | 58.1% | 33.4 | 0.60x | 26.3% |
| **4.5** | **96.5%** | **16.6** | **0.30x** | **30.1%** |
| 5.0 | 96.7% | 16.5 | 0.30x | 0.1% |
| 8.0 | 98.3% | 15.5 | 0.28x | 1.4% |

→ 🔴 **拐点在 4.5px**：过了就进平台（4.5 → 8.0 只再省 1.4%），再往上调等于"白拿观感换空气"。
  取 **5.0** = 落在平台起点 + 留一点余量（4.5~5px 之间还有个小尾巴）。
→ **1px 是光栅化的物理边界**（小于它最多覆盖 1 个像素，抗锯齿还会摊暗），
  但**在这块板上触发 0 次**（最小图形 2.2px）—— 它是极端缩小时的兜底，**不是用来省时间的，别指望它**。
→ 否掉的其他取值：**3px**（0.87x，几乎没省 —— 2~3px 那档描边本来就便宜）；
  **4px**（漏掉 3~5px 里最贵的 4~5px 那半）；**8px**（只多省 2%，却动到已经能看出圆角的图形）。
→ 补充：px 阈值随窗口尺寸 / DPI 自动缩放（窗口大 → 少降级），这正是想要的；
  会变的是"能覆盖多少比例"—— 换一块图形普遍更大的板，同一个 5px 收益会变小，那时按这条曲线重定。

### 被否 / 留作下一步
- ❌ 跳过亚像素图形单做（省 0%）／合并 GraphicsPath（慢 3.5x）／按缩放级别整档切换（底图变点阵）
- ⏭ **交互期降级 + 静止后精绘**（GUI 常见做法）：控件库**没有暴露"正在拖拽/缩放"的状态**，
  要自己挂鼠标钩子 + 静止计时器。静态分级已把 76ms 压到 18ms，先看实机手感再决定。

### 文档
- 新建 `MD文件汇总（AI）/问题&解决方案/缩放卡顿与绘制精度分级.md`
  （量级表 + 根因 + 三张实测表 + **6 条被否方案** + 边界）
- `待办清单.md` 加 **T46**（实机验收，重点：① 顺畅度与观感 ② 放大后恢复成圆 ③ 选点不应变方块）
- `更新日志.md` 加第 14 条；`MD文件汇总（AI）/README.md` 索引登记一行
- 根 `README.md`：关键设计决策加一条 LOD（含"两趟必须在图层循环内部"这条硬约束）
- 巡检脚本**通过**（25 个 md）

---

## 第十八轮（用户："把解析光圈的日志删掉" → 指向 `D:\MyWork\GerberParserTool`）

### 先定位到源头：不在本工程
本工程源码里**没有任何 `Console.Write`**（`KLog.cs` 的注释还专门写着"WinExe 没有控制台，
Console 输出全被丢弃"）。所以那些刷屏来自 **`GerberParserTool.dll`** ——
一跑就刷 `解析行: …` / `移动到坐标: …` / **`光圈D12在位置(…)曝光`** / `钻孔解析行: …`，
一块 8 层板能刷出**几十万字节**（实测 513,935 B 被截断）。
平时被丢弃，但**带控制台 / VS 调试运行时是实打实的噪声** —— 这才是用户看到的。

### 改动（`D:\MyWork\GerberParserTool\GerberParser.cs`，**1398 → 1210 行**）
- **删 38 行**：逐行 dump（`解析行` / `钻孔解析行` / `移动到坐标` / `光圈Dxx在位置(..)曝光` /
  `单独D03*` / `移动指令` / `忽略指令` / `钻孔位置`）+ 过程提示（检测文件格式 / 检测单位 /
  选择钻孔刀具 / 刀具定义 / 切换光圈 / 检测到光圈选择 / 圆·矩·椭圆光圈定义 / 解析完成）
- **空 if 的收尾**：`if (line.Length > 0 && !line.StartsWith("%") && line != "M02*") { Console.WriteLine(忽略指令) }`
  共 **3 处** —— 删掉打印后只剩空块，**连 `if` 一起删**（含上方注释行）
- **`PrintSummary()` 清空方法体（142 行）**：它打印「光圈 ID / 形状 / 尺寸 / 坐标数量 / 钻孔详情」整段表。
  **保留方法签名** —— 本工程 `MainForm.cs:2526` 在调用它，且不清楚该 dll 有没有别的引用方
- ✅ **保留 5 处异常信号**：`警告: 未定义的光圈` / `警告: 在位置(…)有曝光指令但无激活光圈` /
  `警告: 未找到刀具` / `未识别的光圈定义` / `计算矩形尺寸出错`（量小、是排查线索）

### 做法：脚本批量删（不为 88 处 Console 写 88 次 Edit）
写了 node 脚本，按"**trim 后整行精确相等**"匹配删除，并让脚本报告**未匹配的模式数**。
结果：删除 38 行、**31 个模式全部命中、0 个未匹配** ✓（比逐个 Edit 快且可核对）

### 验证（三重，都是"硬证据"而非推断）
1. **直接查 dll 的字符串常量**（.NET 用户字符串堆是 **UTF-16LE**，可在 dll 字节里搜）：
   15 个"应消失"的日志串**全部查不到**、5 个"应保留"的警告串**都在** ✓
2. **功能等价**：同一块 8 层板重跑解析统计，**图元总数 20,188 逐个文件一致** ✓
3. 本工程 `dotnet build` **0 warning / 0 error** ✓

### 三个重要发现（都已写进文档）
- 🔴 **`GerberParserTool` 有两份源码**：`D:\MyWork\GerberParserTool\`（用户给的、本次改的）与
  `D:\CSharpWorkBase\ClassLib\GerberParserTool\`。**改前 MD5 逐字节相同** → 副本关系。
  旧那份**没改**，将来若有人从它编译替换 `lib\`，日志会回来。已在 README / 更新日志 / 待办标注。
- 🔴 **该目录不在 git 下**（`D:\MyWork\GerberParserTool\.git` 不存在，`D:\MyWork` 也不是仓库）
  → **改前必须先备份**（已备份 `C:\Users\59538\.workbuddy\tmp\GerberParser.cs.bak`，55,890 B）。
- 附带查明：原来 `lib\GerberParserTool.dll` 是 **Debug/net48** 编的
  （28,672 B / 09-18 15:04:14，与 `CSharpWorkBase\...\bin\Debug\net48` 的时间戳大小完全吻合）
  → 我**按原状也编 Debug**（**21,504 B**），不擅自换成 Release（Debug 版便于调试步进）。
  ⚠ `lib\` 下**没有** `GerberParserTool.pdb`（原来也没有）—— 若以后要在 VS 里步进 dll，得把 pdb 也拷进去。

### 文档
- `更新日志.md` 加第 15 条；`待办清单.md` 加 **T47**（你只需跑一次看是否还刷屏）
- 根 `README.md` 依赖表：源码路径 `D:\CSharpWorkBase\...` → **`D:\MyWork\GerberParserTool\`**、
  体积 28,672 → **21,504 B**、并写明"另有一份旧副本，别从它编译替换"
- `.workbuddy/memory/MEMORY.md` 环境红线补一条（源码位置 + 编译替换流程 + 两份源码的坑 + 无 git）

---

## 第十九轮（用户问：为什么默认 150x、上限 500 能否改到 1000）

### 用户原话
> 为什么 `bin\x64\Debug\Broad\q02578f045a00` 缩放默认是150x？缩放最大只能到500，
> 能否给个配置文件，我来改到1000？

### 问题一：默认 150 —— 不是"设错了"，是 **`Scale` 的语义被状态栏 `×` 号搞歧义了**
- `UpdateScaleLabel()`（`MainForm.cs:2623`）写的是 `$"缩放 {kWindowControl1.Scale:F3}× | 可见 …"`。
  **`Scale` 是"世界单位 → 屏幕像素"的比例（`1.0` = 1:1），不是"倍数"** ——
  那个 `×` 只是装饰，真实意思是"1 个世界单位（mm）占 150 个屏幕像素"。
- 默认值由 `FitToWindow` 算（`ResetViewToCenter` 触发）：
  `scale = min(可用宽/内容宽, 可用高/内容高) × FitMarginRatio(0.9)`。
  **实测那块板**（用 lodstat 解析 `Broad\q02578f045a00`，**只读**）：
  | 项 | 值 |
  |---|---|
  | 规模 | 6 文件 / 图元 **19774** / 光圈组 31 |
  | 包围盒 | **8.7 × 5.2**（世界单位）← 很小的一块板 |
  | 1200×800 画布下 scale | **124.0** |
  → 用户窗口更大（约 1600×900）→ `min(1600/8.7, 900/5.2)*0.9 ≈ 155` ≈ **150** ✓ 完全吻合。
  **结论：小板铺满大画布，默认 scale 必然大，逻辑上没错；错的是显示写法。**

### 问题二：上限 500 → 做成配置项
- 上限来自控件库 `KWindowOptions.MaxScale = 500`（`D:\MyWork\Common\Korey.SmartWindow.API.md:236`），
  而 `KViewport.MaxScale` 是**可写属性**（`:212`）→ 不用改控件库，本工程设一下即可。
- 改动（3 处）：
  1. `App.config` 加 `<appSettings><add key="ViewMaxScale" value="500" /></appSettings>`
  2. `MainForm.cs`：`ApplyViewScaleLimits()` + `ReadPositiveSetting()`，在 `MainForm_Load` 里调
     （`EnsureDefaultProjectRoot()` 之后）
  3. `csproj` 补 `<Reference Include="System.Configuration" />`
- ⚠ **踩到一个引用坑**：项目原来**没有** `System.Configuration` 引用却一直编译通过 ——
  因为 `Settings.Designer.cs` 用的 `ApplicationSettingsBase` 在 **`System.dll`** 里，
  而 `ConfigurationManager` 在 **`System.Configuration.dll`**（两个不同程序集）。
  第一次编译直接 `CS0103: 当前上下文中不存在名称"ConfigurationManager"` → 补引用后通过。
- 容错口径：缺失 / 非数字 / ≤0 / NaN **一律忽略**（保持控件库默认），
  与 `KViewport` 自己"≤0 或 NaN 会被忽略"一致 —— 绝不能因为写错值就把上限设成 0。
- **持久生效要改源码的 `App.config`**（编译时生成 exe 旁的 `exe.config`）；
  直接改 exe 旁的会被下次编译覆盖。已在配置注释里写明。

### 验证
- `dotnet build` **0 warning / 0 error**（补引用前 1 个 CS0103，补后通过）
- **读了编译产物** `gpv4_build\GerberParserSmartV4.0.exe.config` 确认 `<appSettings>` + `ViewMaxScale=500` 已落地
- csproj diff = **纯 1 行新增**；`App.config` / `csproj` / `MainForm.cs` 行尾均 `w/crlf` ✓
- 那块板的数据是用临时工具只读解析的，**没写进任何工程文件**

### 文档 / 留给你决定
- `更新日志.md` 加第 16 条；`待办清单.md` 加 **T48**（改 value → 重启 → 看能否越过 500）
- 根 `README.md` 关键设计决策加一条（含"Scale 不是倍数"这条口径澄清）
- `.workbuddy/memory/MEMORY.md` 补一条（缩放上限配置项 + 两个程序集的坑）
- ⏭ **留给用户定**：状态栏那句 `缩放 {Scale:F3}×` 的 `×` 有歧义 ——
  建议改成 `缩放 {Scale:F3} px/单位`（一行改动）。**没擅自改**，等他说。
  另外他若其实想要"打开时不铺满（固定 1:1）"，那是另一个需求，也等他明确。

---

## 第二十轮（状态栏文案 + 修「悬浮浮层下方图形闪烁」）

### ① 状态栏文案（用户明确要求）
`缩放 {Scale:F3}×` → **`缩放 {Scale:F3} px/单位`**（`UpdateScaleLabel`，`MainForm.cs`）。
根源就是上一轮他问"为什么默认 150x"——那个 `×` 会被读成"倍数"。

### ② 浮层下方图形闪烁（用户反馈）—— 读控件库源码定位，不是猜
**两个事实拼起来**（都在控件库 `D:\MyWork\Common\Korey.SmartWindow.WinForms\` 里）：

1. 浮层 `OverlayToolbar` 是**半透明**的（`BackColor` 带 alpha）→ WinForms 的"透明"是**绘制模拟**：
   浮层重绘时先回调**父控件（画布）的 `OnPaintBackground`** 把底下那块内容画出来、再叠半透明底
   （`OverlayToolbar.cs:14-22` 的"透明链"）。**也正因如此**，`KWindowControl` 把内容画在
   **`OnPaintBackground`** 而不是 `OnPaint`（`KWindowControl.cs:23-31` 专门标了"★ 最关键的一处非常规设计 ★"）。
2. **两边双缓冲不一致**：
   - 画布：`KWindowControl.cs:115-119` 有 `ControlStyles.OptimizedDoubleBuffer` ✓
   - 浮层：`OverlayToolbar.cs:57` **只设了 `SupportsTransparentBackColor`**，没有双缓冲 ✗
     （`Panel` / `FlowLayoutPanel` 默认 `DoubleBuffered = false`）

**⇒ 浮层那两步（补画父内容 + 叠半透明底）是直接往屏幕上画的** →
在"父内容只画了一半"的窗口期里露出中间态。
画布本体有缓冲所以不闪，对比之下那块区域更显眼 —— 与用户描述（"**下**绘制的形状会闪烁"）完全吻合。

### 🔴 但真正的主因在更前面一层（第二轮才查到 —— 自我更正）
`KWindowControl.OnViewportChanged`（`KWindowControl.cs:233-238`）用的是 **`Invalidate()`（无参）**：

    private void OnViewportChanged(object sender, EventArgs e)
    {
        Invalidate();          // ← 无参 = invalidateChildren: false
        ...
    }

**它不失效子控件**，而浮层**正是子控件** → **滚轮 / 平移时浮层压根不重绘**，
它覆盖的那块会**停在缩放前的旧画面上**，直到别的原因（鼠标移进去、显隐切换）才重绘
= 用户看到的"浮层下方内容**滞后 / 跳一下**"。
> 双缓冲只能让"重绘时不露中间态"；**压根不重绘 = 一直显示旧内容**。这条更根本，**两条都要修**。

**修法（两处，都在本工程侧；不动共享控件库）**：

**① 让浮层跟着视口变化重绘 —— 主修复**
`OnViewChanged`（`MainForm.cs:774`，原本只刷状态栏）补一句
`kWindowControl1.Invalidate(true);`（参数 `true` = **连子控件一起失效**）。
反复 Invalidate 只是标脏、下一个 `WM_PAINT` 会合并成一次，不会多画一遍；
也不会递归（浮层重绘只回调画布的 `OnPaintBackground` 补底，**不改视口**）。

**② 给浮层开双缓冲 —— 次修复**
- `private sealed class DoubleBufferedOverlayToolbar : OverlayToolbar`
  → 构造函数里 `DoubleBuffered = true;`
- 两处 `new OverlayToolbar()` → `new DoubleBufferedOverlayToolbar()`（顶部工具栏 + 图层容器）
- **为什么必须用子类**：`Control.DoubleBuffered` 是 **protected**，外部设不了；
  改控件库的构造函数会**外溢到所有引用该 dll 的工程**（`Korey.SmartWindow` 是共享自研库）。
- 已核对接口：`AttachOverlayToolbar(Control toolbar, ToolbarEdge edge)`（`KWindowControl.cs:354 / 360`）
  参数类型是 **`Control`** → 接子类没有任何问题。
- 代价：浮层各多一块内存位图（可忽略）；**无观感变化**（半透明仍是半透明）。

### 验证
- `dotnet build` **0 warning / 0 error**；`MainForm.cs` 行尾复验 `w/crlf` ✓
- （实机闪烁是否消失需用户确认 —— 这个只能肉眼看）

### 文档
- 新建 `MD文件汇总（AI）/问题&解决方案/悬浮浮层下方图形闪烁.md`
  （现象 / **根因含控件库"透明链"机制** / 方案 / **5 条被否方案** / 一个独立未做的候选优化）
- `更新日志.md` 第 17 条；`待办清单.md` 加 **T49**；`MD文件汇总（AI）/README.md` 索引登记一行
- 根 `README.md` 关键设计决策加一条（含"**别改共享库 `Korey.SmartWindow`**"这条红线）
- `.workbuddy/memory/MEMORY.md` 补一条

### 第三处（用户回"改"批准）：`RefreshKWindow()` 去掉同步阻塞
`Refresh()`（= `Invalidate(true)` + `Update()`，**同步**画完才返回）→ **`Invalidate(true)`**（异步标脏）。
⚠ **自我更正**：我最初举的例子"滚轮快速连发"**不准确** —— 滚轮走的是控件内建
`Viewport.Changed → OnViewportChanged → Invalidate()`，**不经过 `RefreshKWindow()`**。
这次改动的实际收益落在"**数据变化后的刷新**"这条路径上
（选点 / 切图层 / 清空选点等 **25 个调用点**），**不是滚轮手感**。

---

## 第二十一轮（用户怀疑：`Porsche Mobileye B STI RH-q02578f045a00` 解析出的尺寸太小）

### 结论：**是解析代码的问题，图纸没问题** —— Gerber 路径从不做单位（MO）换算

用户报的那个目录尺寸小得离谱。**先量后判**，写了两个只读探针
（`C:\Users\59538\.workbuddy\tmp\gerber_unit_probe.py` / `gerber_by_project.py`），
扫了 `选点软件\Gerber文件` 下全部 4 个工程根 / 8 个 Gerber 工程：

| 工程 | 声明 | `%ADD` 单位 | 代码算出的板尺寸 |
|---|---|---|---|
| Porsche Mobileye **LH**-q02578f044a00 | `%FSLAX35Y35*%` + `%MOMM*%` | mm | 210.594 × 129.999 ✓ |
| Porsche Mobileye **RH**-q02578f045a00 | `%FSLAX26Y26*%` + **`%MOIN*%`** | **inch** | **8.279 × 5.118** ✗ |

🔴 **同一块板的左右件，物理尺寸必须相同**（全部坐标口径两边都是 **228.000 × 139.999 mm**），
但代码给出的结果差 **25.40 倍** —— 正是 inch→mm 的换算系数。这就是最硬的对照实验。

### 根因（`D:\MyWork\GerberParserTool\GerberParser.cs`）
Gerber 的"数值 → 物理尺寸"要过**两道**换算，代码只做了第一道：

1. **FS（格式声明）**：`%FSLAX26Y26*%` → 除 `10^6` ✅ 做了（`:832` / `:854`）
2. **MO（单位声明）**：`%MOIN*%` → 再 `×25.4` ❌ **Gerber 四条通路一次都没做**

硬证据：全文 `25.4` 只出现 **两处**（`:433` 钻孔刀具直径、`:466-467` 钻孔坐标），
**全在 `ParseDrillData`/钻孔分支**；四条 Gerber 通路（`:556` X45 / `:693` X44 / `:765` X26 / `:902` X35）**零处**。
（`DetectFileType:325` 的 `StartsWith("%MO")` 只用来判类型，**不读值**。）

**第二个独立证据（光圈尺寸）**——同一块板左右件的光圈定义逐一对得上：

| | LH（公制） | RH（英制） | RH × 25.4 |
|---|---|---|---|
| 某焊盘 | `0.739700` mm | `0.029122` in | **0.73970 mm** ✓ |
| 另一焊盘 | `1.439697` mm | `0.056681` in | **1.43975 mm** ✓ |

不换算的话，RH 算出的是 **0.029 mm 的焊盘**（工艺下限 ~0.1mm），物理上不存在。

### 🔴 同族缺陷（本轮一并暴露，都还没改）
1. **FS 探测是 4 个硬编码字符串**（`:493-496` 只认 `X45Y45` / `X44Y44` / `X26Y26` / `X35Y35`）
   → **没有 `2:5`**。实测 `Porsche LH` / `Skoda LH,RH` / `Gerber_PCB1` 共 **26 个文件全是 `FS=2:5`**，
   `ParseGerberData` 一律返回 `CreateError("未检测到格式声明")` → **整层被丢**。
   （所以用户现在能打开 RH、打不开 LH —— 可用来验证本条。）
2. **光圈尺寸同样不换单位**（`ParseApertureDefinition:1039-1090`）。
3. **只统计 D03 曝光点**，`G36/G37` region 完全不认 → 纯 region 层（`rout.gbr` / `ssb.o` / `sst.o` /
   `past-t.o` / `GKO` / `GTO` / `GDL` / `GBO`）坐标数 = 0 → 包围盒是空，画不出东西。
4. `Reset():1117-1123` **不重置 `isDrillMetric`**（只清 `apertureDict` / `currentAperture` / `lastX/Y`）
   → 钻孔单位状态跨文件泄漏（次要）。

### ⚠️ 潜在后果（要先查）
RH 目录是 `MOIN`，用它选出来的点**坐标全是 1/25.4 的值**。
若这些 json 已下发产线，**插针机会把针全打在板子左上角一小块区域内**。
→ 建议 grep `circles.json` 里有没有 `q02578f045a00` 的记录。

### 修复方向（未动代码，等用户拍板）
最小改动：加 `isGerberMetric` 字段，头部读 `%MOMM*%` / `%MOIN*%`，
四条 Gerber 通路把 `/100000.0` 改成 `/100000.0 * (isGerberMetric ? 1 : 25.4)`；
`ParseApertureDefinition` 同样处理。更稳的做法是**不硬编码 FS**、
把 `%FSLAXabYab*%` 解析成 `(intDigits, decDigits)` 统一除 `10^dec`（顺带解决 2:5 那个坑）。

### 环境事实
- `Reflection.Assembly.LoadFrom` **被安全策略拦**（与 `Add-Type` 同类）→ 想直接跑 `lib\GerberParserTool.dll`
  拿"代码真实输出"这条路走不通，只能用复刻逻辑的 Python 探针 + 逐行对照源码。
- 探针脚本都在仓库外 tmp，**未写任何工程文件**。

---

## 第二十二轮（用户批准：彻底优化 GerberParserTool.dll 源码）

用户说"开始改，这里还有其他格式的 Gerber 文件供你训练和验证，彻底优化这个 dll 源码"。
**已改 → 已全量验证 → 已编译 → 已装进本工程 `lib\`。**

### 备份（改动前）
- 源码整目录：`D:\MyWork\GerberParserTool_bak_20260921\`
- lib 里的旧 dll：`C:\Users\59538\.workbuddy\tmp\GerberParserTool.lib.bak.dll`（21504 B）
- 回退：`git checkout lib/GerberParserTool.dll`

### 改了什么（`GerberParser.cs` 整体重写，1209 → 1590 行）
| # | 缺陷 | 修法 |
|---|---|---|
| 1 | Gerber 路径**从不读 `%MO*%`** | 新增 `_unitToMm`；**坐标与 `%ADD` 光圈尺寸**都乘（只乘坐标会让英寸件的焊盘还是 1/25.4） |
| 2 | FS 是 4 个硬编码字符串 | 正则解析 `%FS[LT]?[AI]?X<int><dec>Y<int><dec>`，任意组合都成立；顺带支持 **TZ 尾随零补零**与 `%FSI` 增量坐标 |
| 3 | **宏光圈 `%ADD<n><MacroName>` 完全不认** | 新增 `%AM` 解析 + 宏体 primitive 外接框求和（支持 1/2/4/5/6/7/20/21/22 号 primitive）→ 转 `Rectangle` |
| 4 | `G36/G37` region 完全不认 | 新增 `FilledArea`：区段内 D01 累积成轮廓，`G02/G03` 按 **5° 采样**成折线 |
| 5 | `D01` 线条完全不认 | 新增 `Stroke`（两端点 + 笔宽 + 笔形状） |
| 6 | `Reset()` 不重置 `isDrillMetric/isDrillFile` | 补上；Excellon 改为读 `METRIC,LZ,0000.00000` 格式声明（旧版硬编码"英制一律除 10000"） |
| 7 | 未识别光圈逐条刷 Console（一个板子上万行） | `Warn()` 去重，同类只报一次 |

### 🔴 三个踩过的坑（都是靠实测数据定位的）
1. **`%ADD` 正则不能写成 `([A-Za-z]*)(?:,(.*?))?`** —— 宏调用 `%ADD41ROUNDEDRECTD41*%` 的类型 token
   后面**紧跟数字**，字母类会把 "ROUNDEDRECTD" 吃光 → 剩下 `41*%` 无法闭合 → 整条 ADD 失配。
   正解：只切"编号 + 其余"，由 `ParseAdd` 再判断逗号两边是什么。
2. **宏体必须按 `*` 分隔，不能按行分隔** —— CX430 的宏是跨行写的（`4,1,4,` 单独一行，坐标在后面几行）。
   按行切会把 `4,1,4,` 当成一条完整 primitive。
3. **code 4/5 多边形的参数 2 是顶点数，坐标从参数 3 才开始** —— 从 2 开始取会把"顶点数"
   当成第一个顶点的 x 坐标，外接框凭空多出一截（IconRect 会变成 4.2mm 宽）。

### 验证（harness 在 `C:\Users\59538\.workbuddy\tmp\gtest\`，**直接调真实 dll**，不是模拟）
81 个文件前后对比 + 20 条断言 → **PASS=20 / FAIL=0**：
- 全库曝光点 **78632 → 84271（+5639）**，增量**正好等于**独立统计出的宏光圈 D03 数
- Porsche RH（`MOIN`）8.28×5.12 → **210.276×129.999**，与 LH 件（210.594×129.999）对上 ✓
- CX430 LH/RH（`MOIN`）6.20×5.16 → **157.53×131.18** ✓
- GDD 曝光点 4 → **437**（IconPlus / IconRect2 两个宏找回）
- 新增数据：region **25829** 个 / **144.7 万顶点**，stroke **187103** 条
- 回归确认：**曝光点只增不减**；`MO=MM` 且点数未变的层尺寸一字未变；失败文件仍是原来那 4 个
- 上位机 `GerberParserSmartV4.0` 重新编译 **0 warning / 0 error** → API 向后兼容
- 性能：81 文件 **1474ms → 1810ms（+23%）**，代价换上面那些数据

### ⚠️ 更正我上一轮的一个**错误结论**
我上一轮说"FS 探测没有 2:5 → Porsche LH / Skoda LH,RH / Gerber_PCB1 共 26 个文件报未检测到格式声明"。
**这是错的**：我的 Python 探针把整数位写死成 `"2:"`，实际这些文件是 **`%FSLAX35Y35*%`（3:5）**，
**正好在旧代码的四个硬编码字符串里，一直解析正常**。实测样例库 FS 只有 4 种：
`X35Y35`(27) / `X26Y26`(25) / `X44Y44`(13) / `X45Y45`(8)，旧代码全都能覆盖。
（硬编码仍是隐患 —— 换家厂商的 2:5 / 4:6 就会整层丢，所以照样改成通用解析。）

### 🔴 下一步（未做，等用户拍板）
**上位机还没消费 region / stroke** → 丝印（GTO/GBO）、板框（GKO/GD1/GG1/GM2）在画布上**仍是空白**。
数据已在 `ParseResult.FilledAreas` / `.Strokes`，上位机需要：
1. `TryParseLayerFile` 把 `result.FilledAreas` / `.Strokes` 存进 `LayerInfo`；
2. `DrawGerberApertures` 里按图层颜色画（region → `FillPolygon`，stroke → 带笔宽的 `DrawLine`）；
3. 🔴 **必须做 LOD**：全库 144.7 万顶点 / 18.7 万条线，不分级在整板视图会卡死；
4. 包围盒可选换 `ViewportHelper.CalculateBoundingBox(ParseResult)`（含 region + stroke）。
   ⚠ GD1（钻孔图）的描边范围是 646×428 mm，板子本体只有 76×101 mm，用新重载会把视图拉大 ——
   这是"如实反映"（那层确实有板外的钻孔表），但要不要用要用户定。

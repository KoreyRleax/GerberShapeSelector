# 待办清单

> 维护规范见 `MD文件汇总（AI）/待办/README.md`。**完成/作废都保留条目并标注，不删除。**
> 排版约定：按状态分节；表格连续无空行；单元格内不换行（用 `<br>`）；状态五档
> `✅ 已完成`／`🟡 待验收`／`⬜ 待推进`／`⏸ 暂缓`／`❌ 已作废`。
> 最后更新：2026-09-21

## 一、待你操作 / 决策

| # | 待办 | 状态 | 来源 | 备注 / 详情 |
|---|---|---|---|---|
| T01 | 编译并实机验证 2026-09-21 三轮改动 | 🟡 待你操作 | 2026-09-21 会话 | 改动落在 `ShapeQuery.cs` + `MainForm.cs`；AI 侧已用 `dotnet build` 编译 7 次（0 warning / 0 error），**实机验证仍需你本地做**。验收清单见 `MD文件汇总（AI）/任务&需求/R-20260921-01-多图层选点与图层顺序.md` 的"验收目标"段 |
| T02 | 决定诊断日志的去留 | ⬜ 待你决定 | 2026-09-21 排查需要 | `MainForm.LogHitDiagnostics()`（每次选点写一行）与 `LogLayerOrder()`（载入工程/置顶时写一行）是排查"选不到想选的那层"时加的，**排查完可整段删除**（两个方法 + 两处调用点，不涉及任何判定逻辑）。你点得越多日志涨得越快，长期留着会拖大 `logs/` |
| T13 | **下游能否接受 `circles.json` 里同一坐标出现多条记录？** | ✅ 已裁定 2026-09-21 | 2026-09-21 更新 README 时发现 | ✅ **用户 2026-09-21 裁定：不被允许**（插针机按坐标定位 → 同坐标多条 = 重复插针）。处理方式选了**"保存时弹窗"**而不是"改模型 / 自动去重"：检出即列出冲突明细并阻断，由用户选「返回修改」或「仍然保存」。已实现 → 见 T45 与 `MD文件汇总（AI）/任务&需求/R-20260921-06-保存前同坐标重复点检查.md`；`README.md` §1.4 与 §五-1 已同步改写。<br>（原结论：模型"不同图层、同坐标是两个点"各占一条记录，与 2026-09-19 稿"同坐标两次 = 插两次针属交付缺陷"矛盾） |
| T31 | 编译并实机验证「图层容器顶部栏（全选 / 取消全选）」 | 🟡 待你操作 | 2026-09-21 会话 | 改动**只在 `MainForm.cs`**（新增 `LayerRowLayout` / `LayerHeaderControl` / `SetAllLayersVisible`，改 `RefreshLayerPanel`）；AI 侧 `dotnet build` 2 次 0 warning / 0 error。**重点看两处**：① 表头「置顶」二字与下面每行 ↑ 图标是否**同列**（唯一只能肉眼看、且随 DPI 字体度量可能变）② 6 层全勾时点方框是否**一次**全部取消。18 条验收清单见 `MD文件汇总（AI）/任务&需求/R-20260921-02-图层容器顶部栏与全选.md` |
| T32 | 半选态要不要保留 | ⬜ 待你决定 | 2026-09-21 追加设计 | 表头方框做成了**三态**（全勾 / 一横=半选 / 空）。半选是 AI 主动加的：两态在"6 层勾了 4 层"时会显示成空或勾，**属谎报**。若你觉得多余，删 `LayerHeaderControl.OnPaint` 里 `else if (_anyChecked)` 那一段即可（点击行为不受影响，它只影响显示） |
| T37 | 编译并实机验证「h1/h2/h3 选点划分 + 合并」 | 🟡 待你操作 | 2026-09-21 会话 | 改动在 `MainForm.cs`（`HeaderType` / 三个 h toggle / 合并模式 / 游离选点收编）+ `ParamsForm.cs` 与 `ParamsForm.Designer.cs`（h2 / h3 两行颜色）+ `Properties/Settings.settings` / `Settings.Designer.cs` / `App.config`（`HeaderH2Color` / `HeaderH3Color`）；AI 侧 `dotnet build` **5 次** 0 warning / 0 error。**重点看四处**：① 工具栏加了 4 个按钮后**是否换行**；② 白/黄/绿三色在纯黑画布上能否一眼分清；③ 合并出的点**能否点中并取消**；④ 「参数设置」里三行颜色能否都改、改完画面是否**立刻**换色。**23 条**验收清单见 `MD文件汇总（AI）/任务&需求/R-20260921-03-选点划分h1h2h3与合并.md` |
| T38 | 合并后**原点保留**还是**替换** | ⬜ 待你决定 | 2026-09-21 移植 V5 时 | 现在照 V5：合并 = 在两点中点**新增**一个点，**原来两个点保留**（选点 +1）。若你要的是"两点换一个中点"（-1），在 `HandleMergeClick` 生成新点后把 `first` / `second` 取消选中并移出 `_selectedCircles` 即可（约 2 行）。见 `MD文件汇总（AI）/任务&需求/R-20260921-03-选点划分h1h2h3与合并.md` §六-6 |
| T39 | **下游要按 h1/h2/h3 分开拿点吗？** | ⬜ 待你确认 | 2026-09-21 移植时 | 当前 `circles.json` 仍是**一个文件**，只在每条记录上加了 `Header` 字段（v2.4）。若插针机上位机需要"h1 的点一个文件 / h2 一个文件"或按头分目录，那是导出端的另一条需求（顺带要定：文件命名、是否共用同一个工程目录）。**未擅自拆文件** |
| T41 | 插针机读不了本项目 json —— **改插针机**（已定夺） | 🟡 待验收 | 2026-09-21 用户反馈"插针机解析 json 报错在 json_value.cpp" | 根因已定位（**不是 jsoncpp 的 bug**）：本项目 `Shapes` 顶层是 `Circles/Rectangles/Ovals`、头类型在记录的 `Header` 里；插针机 `Common\CADImportDlg.cpp`（2026-08-18 从老项目 V5 移植）按 `Shapes:{h1:{Circles…}}` 解析 → 对**数组**调 `isMember` → jsoncpp `find` 前置断言抛 `Json::LogicError`，无人 catch → 崩。**用户 2026-09-21 裁定：改插针机、且不做 V5 兼容（V5 后续将废弃）**。**代码已改**：`CADImportDlg.cpp` 新增 `ParseShapeArray`（按 `Circles/Rectangles/Ovals` 直取 + 头类型读记录 `Header`）、`ParseJSONContent` 拆薄壳兜异常、各处补判类型，`.h` 加 `ParseJSONContentImpl` 声明；R9/RS/R5/R6 分支行为未动。⚠ **插针机侧未编译**（不在本机工程里），需你在产线工程编译后按 5 条验收目标实机验证。全文：`MD文件汇总（AI）/问题&解决方案/插针机导入json崩溃（Shapes顶层按形状分组）.md` |
| T42 | 隐藏图层的点**要不要**导出给插针机 | ⬜ 待你决定 | 2026-09-21 查 T41 时顺带发现 | 当前规则 R1 = "隐藏 = 不画 + 不选 + **数据保留**"，写端**不做显隐过滤** → 操作员藏掉某层再保存，插针机照样拿到那层的点。若现场期望"藏起来的层不进模板"，那是**本项目**的改动（保存端加 `IsShapeLayerVisible` 过滤），与 T41 的格式兼容是两件事 |
| T40 | 编译并实机验证「移除模板文件保存路径设置」 | 🟡 待你操作 | 2026-09-21 会话 | 改动在 `MainForm.cs`（删 4 个路径方法 + `btnParamSetting_Click` 精简 + 新增 `GetProjectParentBrowseDir`）、`ParamsForm.cs` / `ParamsForm.Designer.cs`（删路径行、三行颜色上移 40 px、窗体高 428→388）、`PublicMethod.cs`（删死方法 `InitializeSettings`）、`Properties/Settings.settings` / `Settings.Designer.cs` / `App.config`（删 `DefaultSavePath`）；AI 侧 `dotnet build` **1 次** 0 warning / 0 error。**重点看四处**：① 新建工程 → 点「保存」是否弹目录选择器、落点对不对；② 在对话框点取消是否**不写任何文件**；③ 打开工程 → 改个颜色 → 点确定 → 再保存，内容是否仍写回**原工程**（这是本轮顺带修掉的缺陷）；④ 参数设置窗体删掉那一行后布局是否顺眼（顶部无空洞、确定按钮位置）。**20 条**验收清单见 `MD文件汇总（AI）/任务&需求/R-20260921-04-移除模板保存路径设置.md` |
| T43 | 编译并实机验证「circles.json v2.5：坐标从 `Positions[]` 提到 `X` / `Y`」 | 🟡 待你操作 | 2026-09-21 用户提议 | 写端 `MainForm.cs` 三条记录（圆 / 矩形 / 椭圆）改为 `X = …` / `Y = …`；读端新增 `ReadJsonPosition`（新格式优先、`Positions[0]` 兜底）与 `TryReadDouble`，三处调用同步改；`Version` → `2.5`。AI 侧 `dotnet build` **1 次** 0 warning / 0 error。**重点验三条**：① 用 **v2.5 新存**的工程重开，选点位置是否与保存时一致（**旧工程 v2.0~2.4 已不兼容** —— 打开后坐标全读成 0、点堆在原点，属预期；⚠ **别对老工程点保存**，否则把"全 0"写回去不可逆）；② 保存后 json 里应**只有 `X`/`Y`、不再有 `Positions`**，且 `Version` 为 `2.5`；③ 插针机用**新版导出**的 json 导入，h1/h2/h3 计数正确。**注意**：插针机的解析已同步改为读 `X`/`Y`（保留 `Positions` 退化分支），所以**必须用新版选点软件导出的 json** 送插针机 |
| T45 | 编译并实机验证「保存前同坐标重复点检查」 | 🟡 待你操作 | 2026-09-21 用户裁定 | 新增 `DuplicatePositionDialog.cs`（已登记 csproj）；`MainForm.cs` 新增 `FindDuplicatePositionGroups` / `DescribeShapeSize` / `BuildDuplicateDetailText` / `ConfirmNoDuplicatePositions`，调用点在 `SaveTemplateToPath` 开头。AI 侧 `dotnet build` **3 次** 0 warning / 0 error；判重算法另在 **.NET Framework 4.8.9345.0** 下用 12 个点自测（检出 4 处 / 9 个点，跨图层、舍入后相等、同层同坐标不同尺寸、负零边界、隐藏/未选中标注全部符合预期）。**重点验三条**：① 框选一片"两层同坐标"的区域后保存 → 是否弹窗、明细是否列全；② 点「返回修改」后 `circles.json` 的**修改时间是否不变**（"一个文件都没写"的硬判据）；③ 没有重复点时保存流程是否**一字不变**。8 条验收目标见 `MD文件汇总（AI）/任务&需求/R-20260921-06-保存前同坐标重复点检查.md` §二 |
| T44 | 编译并实机验收「工程管理改造（工程名自定义 / 拖放加载 / 默认工作路径 / 新建完自动落盘）」 | 🟡 待你操作 | 2026-09-21 用户四项需求 | 改动：新增 `SaveProjectDialog.cs` + `ProjectNameDialog.cs`（已登记进 csproj）；`MainForm`（新建流程重排 + 拖放 + `AppendLayersFromFiles` / `ReplaceLayer` / `EnsureLayerIdentity` + 保存图层拷贝判据改 `IsSamePath`）；`ParamsForm`（加「默认工作路径」行）+ `Settings.settings` / `Settings.Designer.cs` / `App.config`。AI 侧 `dotnet build` **7 次** 0 warning / 0 error。**重点看四处**：① **新建完不点保存**，磁盘上应已有工程目录 + `circles.json`（这是需求 4 的核心）；② 拖入图层后立刻点「保存」，必须写回**原工程**（归属不能被拖放弄丢）；③ 同名替换的三条分支（替换并清该层选点 / 跳过 / **其余层选点不受影响**）；④ **置顶小圆图层后点小圆，应选中小圆层**（本轮修掉的顺序缺陷）。约 35 条验收目标见 `MD文件汇总（AI）/任务&需求/R-20260921-05-工程管理与拖放加载.md` |

## 二、待推进 —— 实现 / 技术债

| # | 待办 | 状态 | 来源 | 备注 / 详情 |
|---|---|---|---|---|
| T04 | `groupBox4`（图层面板）是空面板 | ⬜ 未开始 | 交接文档"已知遗留" | 多图层实际由**画布右上角的图层容器**承担（`BuildLayerPanel()` + `LayerRowControl`，运行时创建），`groupBox4` 至今无任何子控件；底部还留着一组 `rbLayerSelectYes/No`（"底图显示"概念），与"图层"命名容易混。确认无用后可整块删除 |
| T05 | `Program.FileName` 是隐式全局状态 | ⬜ 未开始 | 交接文档"雷区"第 3 条 | 它是静态字段，`MainForm` 之外也有引用；任何一处悄悄改它都会影响别的读取点。真正的状态源应是 `_currentTemplatePath`（工程归属）。**默认值硬编码本机路径那条已修**，这一条仍在 |
| T06 | 无全局异常处理 | ⬜ 未开始 | 交接文档"已知遗留" | `Program.Main` 没挂 `Application.ThreadException`，未捕获异常走 WinForms 默认错误框，信息不落地（`KLog` 里看不到）。建议补上并写进 `KLog` |
| T07 | 旧「加载模板」入口成了死代码 | ⬜ 未开始 | 交接文档"已知遗留" | `btnLoadTemple_Click` / `LoadTemplateFile` / `LoadTemplateFromFolder` **已无任何调用点**；且它的 json 兜底还是老口径（`Directory.GetFiles(folder,"*.json")[0]`），留着容易被误接上线。确认无用后可整段删除 |
| T08 | 模板模式下"选中 / 未选中"视觉同色 | ⬜ 未开始 | 交接文档"已知遗留" | `DrawAllTemplateCircles` 把全部模板点按同一颜色画过一遍，只能靠框选限定与状态栏数量区分。优先级低 |
| T09 | 读 circles.json 仍走 `dynamic`（`LoadJsonFormat`） | ⬜ 未开始 | 2026-09-21 修 `ApplyLayerVisibility` 时发现 | `ApplyLayerVisibility` 已改成 `JToken` 索引器（见 `MD文件汇总（AI）/问题&解决方案/打开工程慢150毫秒（用异常探测字段）.md`），但 `LoadJsonFormat` 里仍是 `dynamic` 点属性 —— **字段缺失会抛异常**，外层 catch 会把整个工程判成"选点文件读取失败"。建议同批改造 |
| T10 | `SetLayerVisible` 这个名字在注释里出现过但无实现 | ⬜ 未开始 | 2026-09-21 读代码时发现 | `LayerRowControl.VisibilityChanged` 的注释写"由宿主转发给 `SetLayerVisible`"，实际宿主是直接改 `LayerInfo.IsVisible` 后调 `RefreshLayerState()`。**只是注释与实现不符**，改注释或补方法名二选一（不影响功能） |

## 三、已确认暂缓

| # | 待办 | 状态 | 来源 | 备注 / 详情 |
|---|---|---|---|---|
| T11 | DPI 感知缺失 | ⏸ 暂缓 | 交接文档"已知遗留" | `App.config` 里没有 DPI 感知声明，"高清版"在缩放显示器上会被系统位图拉伸，矢量优势打折扣。当前使用者机器 DPI 为 100%，不影响 |
| T12 | 多图层"跨图层同名光圈 ID"的语义 | ⏸ 暂缓 | 2026-09-21 会话 | 同类扩散已限定在**同一图层内**（光圈 ID 只在一个 Gerber 文件内有意义）。若将来希望"跨层一起选同名焊盘"，再回来改 `HandleMultipleSelect` 的过滤条件 |

## 四、已完成（保留备查）

| # | 待办 | 状态 | 来源 | 备注 / 详情 |
|---|---|---|---|---|
| T20 | 多图层下"选点只能选到最大圆圈那一层" | ✅ 已完成 2026-09-21 | 用户 2026-09-21 | 根因：`ShapeQuery.HitTest` 只按"点到边界距离"取胜者，而圆内距离 = `len - r` → 系统性偏向大圆。修：命中改成**先比图层叠放、同层再比距离**（最上层无图形则穿透到下层）。全文见 `MD文件汇总（AI）/问题&解决方案/选点只能选到最大圆圈那一层.md` |
| T21 | 图层隐藏后，它的选点仍可见、仍可点中 | ✅ 已完成 2026-09-21 | 用户 2026-09-21 | 两个漏口：① 选点是独立于底图画的第二遍（底图过滤了显隐、选点那两处漏了）② 命中测试只认几何不认图层。修：`IsShapeLayerVisible()` 统一口径，接在绘制 2 处 + 命中 + 框选 + 多选扩散。见 `MD文件汇总（AI）/问题&解决方案/隐藏图层的选点仍然可见可点.md` |
| T22 | 图层状态"用图层名字符串关联"太脆（**第一版修复失败的根因**） | ✅ 已完成 2026-09-21 | 2026-09-21 第二轮 | 名字对不上 → 所有图形判成"未知图层" → 命中静默退回纯距离排序 = 又只能选到大圆，且无报错。修：图层的 `Id / 深度 / 是否隐藏` **直接拷进图形对象**（`LayerId`/`LayerDepth`/`LayerHidden`），归属用**光圈对象引用**建立，全链路零字符串匹配 |
| T23 | 图层自定义顺序（容器内「↑」置顶） | ✅ 已完成 2026-09-21 | 用户 2026-09-21 | 点该层右侧的「↑」图标 → 移到最上层并排到列表首行。顺序随 `circles.json` 的 `TemplateInfo.Layers` 保存/还原；配色改用 `ColorIndex`，重排不变色 |
| T24 | 同类扩散只在当前点击那一层内进行 | ✅ 已完成 2026-09-21 | 用户 2026-09-21 | `HandleMultipleSelect` 的过滤加上 `c.LayerId == clickedShape.LayerId` |
| T03 | 图层容器顺序与画面层序的最终形态 | ✅ 已完成 2026-09-21 | 用户 2026-09-21 提出 | 容器原为"首行 = 最先加载 = 画在最底下"，与画面相反；现改为**倒序显示**（首行 = 画面最上层），并给每行加了「↑」置顶图标。见 `MD文件汇总（AI）/任务&需求/R-20260921-01-多图层选点与图层顺序.md` |
| T25 | 重复坐标的图形互相顶替（唯一键缺"尺寸"） | ✅ 已完成 2026-09-21 | 用户追问"重复坐标怎么处理"时由回归日志暴露 | 日志指纹 `已选 2 个 / 复用选点对象 8 个`。修：`ShapeKey` 补尺寸维度 + `usedSelected` 兜底 + `IsSingleShapeInShapes` 补图层维度。见 `MD文件汇总（AI）/问题&解决方案/重复坐标的图形互相顶替.md` |
| T26 | "打开工程"白花 150 ms（异常探测字段） | ✅ 已完成 2026-09-21 | 用户反馈"加载好像变慢了"时定位 | `ApplyLayerVisibility` 用 `try/catch` 兜 v2.3 才有的 `ColorIndex` → 打开老工程时每个图层抛一次异常（6 层 = 150 ms）。改用 `JToken` 索引器。见 `MD文件汇总（AI）/问题&解决方案/打开工程慢150毫秒（用异常探测字段）.md` |
| T27 | 置顶按钮从文字改成图标 | ✅ 已完成 2026-09-21 | 用户 2026-09-21 | 改为几何自绘的向上箭头（不依赖字体），宽 34→24 px，并加 `ToolTip` 补文字说明 |
| T28 | 2026-09-19 的多图层改造（第一阶段） | ✅ 已完成 2026-09-19 | 上一轮会话 | 数据模型加 `Aperture.Layer` / `SelectedCircle.Layer`、图层容器、按图层着色、`TemplateInfo.Layers` 持久化等。逐条记录见 `.workbuddy/memory/2026-09-19.md` |

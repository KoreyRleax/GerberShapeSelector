# 历史需求索引（本体系建立之前）

> 本工程在 **2026-09-21** 才按 RBLAOI 的规则建立"任务&需求"文档体系。
> 之前的改动**没有**需求文档，但改动本身是完整的、可追溯的 —— 索引如下。
> 需要展开时按指针去读；**不重复搬运**（避免两边不同步）。

## 一、2026-09-19 —— 多图层改造（第一阶段）

**要什么**：一个工程目录里可能有多个 Gerber 文件，像 GerberView 那样叠加显示；
图层可勾选显隐；选点要记住自己属于哪一层。

| 项 | 内容 |
|---|---|
| 需求来源 | 用户 2026-09-19 会话指示 |
| 状态 | ✅ 已完成 2026-09-19（AI 侧编译验证通过，实机由用户验证） |
| 详情 | `.workbuddy/memory/2026-09-19.md`（逐条改动记录，含理由与自检） |
| 架构沉淀 | `MD文件汇总（AI）/系统架构&协议设计/系统架构总览.md`、`.workbuddy/memory/MEMORY.md` |

要点（结果）：
- `GerberParserTool.dll` 侧：`Aperture.Layer` + `GerberParser.LayerName`，在 `GetApertureList()` 唯一出口回填
- 本工程侧：`SelectedCircle.Layer`、`ShapeKey(图层+坐标+形状)`、`_layers` 作为权威数据
- UI：画布右上角**图层容器**（运行时创建）、按图层着色、`TemplateInfo.Layers` 持久化（v2.1）
- 顺带修的：`GerberParserTool.dll` 挪进 `lib/`、`Program.FileName` 默认值去掉本机路径、
  `circles.json` 只认这一个文件名、选点对象与图形缓存的"退化键"回填（v2.2）

## 二、2026-09-18 —— 去 Halcon 化 + 性能修复

**要什么**：去掉 Halcon 依赖（授权不允许商业化），改用自研控件库 `Korey.SmartWindow`；
顺带修掉三处 O(N²)。

| 项 | 内容 |
|---|---|
| 状态 | ✅ 已完成 2026-09-18 |
| 详情 | `../Halcon替换为Korey_SmartWindow迁移说明.html`、`.workbuddy/memory/2026-09-18.md` |
| 关键结果 | 每帧重建 15,359 个对象 → 一次构建（省约 2,960 万次比较/次点击）；包围盒缓存（原 2.36 亿次迭代/帧，是"一点翻转就卡死"的根因）；框选去重改 HashSet |

## 三、2026-09-17 —— 接手与整理

| 项 | 内容 |
|---|---|
| 状态 | ✅ 已完成 2026-09-17 |
| 详情 | `.workbuddy/memory/2026-09-17.md`、`../GerberParserV4.0_交接文档.html` |
| 关键结果 | 代码地图与设计决策整理成交接文档；明确 `bin\x64\Debug\Json\` 是**不可再生**的使用者数据 |

## 四、横向资料（不属于需求，但常被引用）

| 文档 | 内容 |
|---|---|
| `../GerberParserV4.0_交接文档.html` | 面向接手开发者：代码地图、数据流、设计决策（第五节）、雷区（第七节）、已知遗留（第八节） |
| `../GerberParserV4.0_功能说明.html` | 面向使用者：功能清单、快捷键、数据格式 |
| `../GerberParserV4.0_代码审查与WPF渲染可行性.html` | 更早的一次代码审查结论（多数性能问题已修） |
| `D:\MyWork\Common\Korey.SmartWindow.API.md` | 自研控件库 API 契约（改画布代码前必读） |
